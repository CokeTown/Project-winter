# 2026-07-22 (수) 작업 일지

## 항공뷰 지도 설계 (디렉터 오더 — 구현 아님)

디렉터: 절차 생성 지도 이미지를 항공뷰 모델링으로 대체 + 노드 클릭 시 근접 환경 표시(레퍼런스: 다크 항공 지도 → 아이소 근접 디오라마). "내가 어딜 정확히 탐사하는지 알 수 있게." 필요한 것·가능 여부·파일 설계·자료구조 설계 요청.

- **현행 실측**: 지도=비오메 캔버스 dataURL(`mapBiomeDataUrl`, 서부/동부 분기) + `MAP_MARKERS` % 좌표 DOM 핀 — 배경과 노드가 **서로 다른 좌표계**라 "% 수작업 튜닝"(코드 주석 자인)이 구조적 부채.
- **설계서**: `design/systems/AERIAL-MAP.md` — 3안 비교(A 풀 라이브 3D=보류/B 베이크 파이프라인=1차 채택/C 근접만 라이브=2차 승격). 판정 근거 = 신규 엔진 기능이 아니라 **기존 자산 재배치**(오프스크린 하네스·셸터 buildEnv 16종·ruin-city 프리팹·#206 맨해튼 격자).
- **자료구조 핵심**: `mapnodes.js` = 도시 **월드좌표가 정본**(SSOT), 이미지 % 좌표는 베이크 도구가 항공 카메라 투영으로 **자동 산출**(`mapnodes.gen.json`) — 지형 바뀌면 재베이크만으로 마커 동행, 수작업 소멸. 뷰 상태 머신 city↔focus 2단.
- **파일 설계**: `ui/mapview.js`(openMapModal 추출 — 신규 기능을 God Object에 얹지 않음, #73 계승) · `render/dioramas.js`(C단계, `live:'env:rooftop'` Strategy) · `tools/bake-aerial.mjs`(시드 고정, icon-semiotic 계보) · `mapBackdrop(city)` Facade(베이크 부재 시 현행 캔버스 **폴백 강등** — 삭제 아님).
- **카메라 규약**: 전도·근접·인게임 전부 동일 아이소 각(골든 yaw0.6/pitch0.42 계열) — "줌인했다"는 착각은 시점 연속성에서 나온다(레퍼런스 사진1→2의 체험 핵심).
- **검증 설계**: 베이크 결정론(같은 입력=같은 PNG) · 골든 2씬 추가 · 좌표 정합 배터리 게이트. ⚠️ **P2 라이브 디오라마는 #212(lodge 플레이키) 함정 지대** — REFACTOR-GUIDE P1(렌더 컨텍스트) 선행 강권, 정적 베이크 단계는 리스크 0이라 계단 순서가 안전.
- **결정 대기 5건〔D〕**: 밤 전도 고정 1장(권고) · 근접 최종형(베이크vs라이브) · 줌 2단(권고) · 데모 포함(권고) · 지역 디오라마 아트 방향.

## 항공뷰 지도 개정 1차 — 실시간 확정 (디렉터 레퍼런스 2차)

디렉터: "실시간성을 원해(날씨·해 지는 정도) · 1번 지도→노드 클릭→2번 항공뷰에서 선택 · 넥페 데모 필수 · 실 모델링+픽셀화 보고 결정."

- **초판 B안(베이크 1차) 폐기** — 실시간 확정. 베이크는 lowSpec 폴백으로만 강등. 레퍼런스 판독: ①도시 항공+노드 그래프(선택 노드 카드=위험도 해골·이동시간·수집품·날씨, 하단 출발) ②노드 근접 항공 라이브(눈·난로 불빛·시간대)에서 출발 확정.
- **아키텍처 전환 — 단일 라이브 씬 + 카메라 2상태**: 전도/근접 이원 구조를 버리고 도시 디오라마 씬 하나에서 카메라가 overview↔focus를 연속 돌리 줌. 근거 = 줌 연속성(레퍼런스 체험의 핵심)·날씨/시간 동기화 단일점·씬 2벌 관리 소멸. 디테일 예산 = 저폴리 인스턴싱+안개+**본편 동일 픽셀화 포스트**(픽셀화가 저디테일을 가리는 배당 — 디렉터 판단 기준과 일치). 노드 오버레이는 DOM 런타임 투영(mapnodes 월드좌표 SSOT 유지, gen.json 불요 폐기).
- **선행 요구 명시**: applyTimeLighting/날씨 적용의 (scene,ctx) 인자화 — **REFACTOR-GUIDE P1의 부분 착수가 이 기능의 전제**. 기능이 리팩터를 견인하는 구도.
- **단계 재편(넥페 역산)**: S1 프로토(서부 미니 권역+시간대×날씨 캡처 매트릭스 = 디렉터 판단물, 최우선)→S2 프레임워크(mapview 추출·오버레이·출발 연결)→S3 데모 유입→S4 QA(모바일 캡 실측·골든 시드+hideDynamic·소크). 총 5~7세션, S1 판정 8월 중 필요.
- **결정 잔여 3건〔D〕**: 줌 2단(권고) · S1 아트 판정 · 경로 점선/「경로에 추가」 노출 수위(1차=단일 목적지 연출만 권고, 다중 경유는 별도 기획).

## 항공뷰 S1 프로토타입 구현 (디렉터 "응 해봐" — `map-aerial` 브랜치)

- **구현**: `render/aerialproto.js`(~250줄) — 도시 디오라마(저폴리 인스턴싱 680블록·도로 격자·주거/슬럼 권역 드레싱·건물 표면 부착 창문 140·화톳불·적색 장애등·낙설 900) + 시간대 조명 키프레임 8개(밤 sun=달 재해석 0.28) + 눈 시 지면·도로·지붕 3면 설원 스왑 + 카메라 2상태 리그(elev 포함 smoothstep 돌리 줌). **본편 rt→post 파이프 공유**: renderFrame에 분기 — 시뮬 틱은 그대로 돌아 시간·날씨 실시간이 공짜로 성립(AERIAL-MAP 개정 1차 설계 그대로). QA 훅 `__shelter.aerialProto()`.
- **함정 검거 — update 미노출**: 팩토리 반환 객체에 `update`를 빠뜨림 → 분기 진입 후 매 프레임 TypeError → renderFrame 사망 → **캔버스가 마지막 본편 프레임으로 정지**(DOM은 독립 합성이라 "정상 렌더 중"으로 위장 — 1차 캡처 오판독 유발). rAF 프로브(36fps 확인)→에러 채집 프로브로 검거. 교훈: 오프스크린에서 정지 프레임은 살아있는 화면과 구분이 안 된다 — 에러 리스너 선부착이 정석(OPERATIONS §4에 이미 있던 규약 재확인).
- **튜닝 4라운드**(캡처 판독 루프): R1 과암전+창문 공중부유(랜덤 산포→건물 placements 표면 부착) → R2 밤 달빛·hemi 상향, 화톳불 decay 1(물리 감쇠는 이 스케일에서 죽음) → R3 **지면 미적설 검거**(지붕만 스왑 — 설원=달빛 반사판이라 밤 실루엣의 관건) + 샥 최소 반경 4.5 + 눈송이 0.32 → R4 주간 hemi·건물 밝기 재상향. 
- **판단물**: 아티팩트 8컷(focus 슬럼 6 = 아침/황혼/밤×맑음/눈 + overview 2) — 밤+눈 컷이 레퍼런스 무드 최근접(설원 달빛·화톳불 온기·판자촌 실루엣).
- **게이트**: 배터리 102/102 · **골든 17/17(0.114%) — 프로토 추가가 본편 완전 무회귀** · 하드코드 신규 0 (프로토는 로케일 무접점이라 i18n 불변).
- **잔여(S2)**: 노드 DOM 오버레이+정보 카드 · mapview 추출 · 출발 플로우 연결 · 조명 함수 본편 통일(P1 부분) · 프레이밍(권역 회피 반경 20→26 후보).

## S1-b 디벨롭 — 폐허 디테일 + 시간 잠식 (디렉터 오더 2건)

디렉터: "각 모델링이 네모로 퉁쳐지는 게 아니라 폭격 맞아 부서진 디테일 + 시간 흐름에 따른 Over Grown".

- **①손상 원형 6종**: 부지(lot) 300개가 각각 박스 1~6개를 뱉는 구조로 전환 — 온전(16%, 폐허 도시에선 성한 게 예외) · 상층 전단 · 외벽만 남음(파사드 2장) · 층 붕괴(슬래브 사이 공백+잔존 기둥) · 기울어 무너짐 · 전소 그루터기. 전 원형에 잔해 스커트(파편 2~5개) 부착. **박스 1,406개**(부지당 평균 4.7배 분해) — 전부 같은 InstancedMesh라 **드로우콜 불변(2)**.
- **②잠식 = #71 규약 이식**(발명 아님): `years=min(3, 경과일/360)` · 겨울 마른 팔레트(0x6a5f45 계열)×밀도 0.6 · 동일 시드 결정론. 3면 발현 — 지붕(무너진 건물일수록 잘 먹힘: 열린 지붕으로 씨가 든다) · 벽면 담쟁이 띠(연차만큼 상승) · 지면 수풀(잔해 틈). 실측 연동 확인: **5개(0.06년) → 124(1.1년) → 575(3년)**, 겨울 258(0.6배 감쇠).
- **game.js 연결**: `aerialProto().open()`이 실제 `state.day`·계절을 주입(래퍼). `rebuild(day, winter)`는 연차 비교 캡처용.
- **프레이밍 검거(실측)**: focus가 텅 빈 공터로 나와 원인 추적 → 지오메트리·조명은 정상(인스턴스 2,126·판잣집 28·sun 1.0)이었고 범인은 **노드 회피 반경 20**이 focus(dist 30) 시야를 통째로 비워버린 것. **폐허를 보여주는 뷰인데 폐허를 치워둔 꼴** → 반경 11로 축소(노드=잔해에 둘러싸인 빈터). 판잣집 톤 상향(0x4a4438→0x6a6250 계열 3종)·최소 반경 3.5.
- **안개 뷰 적응**: overview(dist 165)에 focus용 밀도를 쓰면 도시가 뿌옇게 씻긴다 → 밀도를 카메라 거리에 반비례(clamp 0.28~1).
- **게이트**: 배터리 102/102 · **골든 17/17(0.114%) — 본편 무회귀 유지**.
- **판단물**: 아티팩트 8컷(연차 3단×focus/overview + 겨울 2). **남은 디벨롭 후보**: 철골 노출·슬래브 각도 등 실루엣 다양화 · 잠식 형상(박스→덩굴 띠/수관) · 원경 밀도 · 화재 그을음 톤.

## 인계 무결성 점검 (디렉터 "다른 채팅에서 조회만 해도 동일 맥락 가능한가?")

주장 전에 실제 체인을 점검 → **구멍 3개 발견·전부 메움**.

1. **캡처 스크립트가 저장소 밖**(가장 큰 구멍) — 판단물 생성기가 임시 스크래치패드에만 있어 다른 세션이 "지금 어떻게 보이는지"를 **재생산할 수 없었다**. 아티팩트는 휘발하지만 생성기는 남아야 인계가 성립 → `tools/aerial-capture.cjs`로 승격(2모드: weather/ruin), **저장소 경로에서 실행 재확인**.
2. **MASTER §2에 S1-b 누락** — 07-22 블록이 S1까지만이라 폐허 원형·잠식·프레이밍 검거가 인계에서 빠져 있었다 → 블록 재작성(진척도에 S2~S4=0% 명시, 재현 명령·함정 3건 요약 포함).
3. **AERIAL-MAP 설계서가 "구현 미착수"** — 실제로는 S1-b까지 구현됨 → **구현 현황 표**(코드 위치·도구·단계별 ✅/⬜) + **재현 절차** + **실측 함정 3건** 절 신설.

점검 결과 정상 확인분: 미커밋 0 · `CLAUDE.md`의 `@docs/MASTER.md` 자동 로드 배선 · MASTER 좌표 브랜치(`map-aerial`) 최신.
