# 2026-07-20 (월) 작업 일지

## 남은 작업 보고 (디렉터 요청)

데모 재수렴 전, 전체 잔여 작업을 WORKLINE 원장 + 실 브랜치 상태 기준으로 정리해 보고. 태스크 보드가 다소 오래돼(#198 도시 결정 07-18 마감·#209 접지감사 마감) 현행으로 재정리:
- **데모 포팅 = 트렁크 재수렴**(demo-vertical-slice가 gd-2.0보다 33커밋 뒤 — 터미널 UI만 못 떼어냄, 관례대로 병합).
- Fable 자율: UI 통일 2차·#73 엔지니어링·#146 결선 트리거·#119 텍스처.
- 디렉터 입력 대기〔D〕: 캡슐·스크린샷·트레일러·#168 GA4·자잘한 결정들.
- 디렉터 콘솔: 스토어 제출·Steamworks(AppID·업적 2·DLC AppID·VDF)·depot.

## 디렉터 UI 배치 8건 (데모 재수렴 이전, 영어 기준)

디렉터 신고: "영어 UI에 아직 이모지 많다(밴드·물·캔) 싹 빼 / Journal 폰트 큼·박스 넘침 / 강제 취침 제거 / Crafting goods 아이콘 빼 / 게이지=영어 단어만·수치는 PDA / PDA 게이지 색 구분 + 실시간 연동 / PDA 지도 웹 드래그 제거." Explore 에이전트로 8건 전수 위치 매핑 후 구현.

- **① 이모지 소거**: `icon()` 폴백을 emoji→공란(부상·개조·스팟 등 PNG 부재분 — 자원/게이지/액션 PNG는 실존이라 이미 아이콘). mode-select 🔒→`icon_sys_locked`, 도료 스와치 🔒→딤 오버레이. (잔여 타이틀 유틸 💾📂는 전용 PNG 부재 — 별도.)
- **② Journal 축소**: `#journal-body` 12→10px·행간 1.9→1.55, 타이틀 15→13px + PDA독 `.report-sec` 축소 규칙 신설(좁은 LCD 박스 넘침 봉합).
- **③ 강제 취침 제거 + 하루 요약 취침 한정** (기전): 05시 collapse 삭제(깨어 있으면 시간만 흐르고 재우지 않음, 유기적 압박). `showDayReport` 큐잉을 자정 롤오버에서 분리 → 취침(sleepUntilMorning)·오프라인 복귀 정산 두 경로에서만. 깨어서 자정 넘겨도 요약 안 뜸. (백그라운드 절전 회복 분기는 유지.)
- **④ Crafting goods 탈아이콘**: 출력·재료 아이콘 제거 → 이름+수량만("Bandage / Cloth 2"). 라이브 39행 전부 무아이콘 확인.
- **⑤ HUD 게이지 = 영어 라벨 + 컬러 바**: 아이콘(.g-ic)·세부 수치(.g-num) 제거, 라벨(.g-name)만. 수치는 PDA 전용. 라이브 "HUNGER" + 바 확인.
- **⑥ 게이지별 색**(디렉터 목업 실현): 허기 머스터드·수분 시안·에너지 그린·청결 라임. HUD `.g-fill` + PDA `.pbar.{key}` 통일, warn 앰버/crit 적색 오버라이드. 라이브 색상 실측.
- **⑦ PDA 실시간 연동**: `renderPDA()`를 0.5s UI 틱에 추가(상태탭 한정 — 탐험 진행바·게이지 실시간, quiet=플리커 생략, 지도/자원탭은 맵 재생성 비용 회피).
- **⑧ 웹 이미지 드래그 고스트 제거**: 전역 `img { -webkit-user-drag: none }` + PDA맵 img `draggable=false`.

**검증**: 라이브(javascript_tool DOM/JS 채널, 영어 부팅) — 게이지 구조·색·크래프팅 무아이콘·저널 10px·PDA 게이지색·드래그 none 전부 설계대로. 빌드·게이트 그린·코어 배터리 102/102(③ 기전 회귀 없음). 커밋 `93bfd75`(UI 7건) + `200fd1b`(③ 취침/요약).

## 남은 것
- **데모 재수렴**(다음): 이 UI 배치 + 33커밋 → demo-vertical-slice 병합 + v1.9.x-demo 재발행.
- 잔여 이모지: 타이틀 유틸 💾📂(전용 PNG 필요) · onerror 폴백(PNG 실존이라 평시 비노출) — 필요 시 후속.
- ③ 취침→요약/자정 롤오버→무요약 타이밍: 디렉터 실플레이 확인(하네스 다초 암전+시간점프 시뮬 불가).

## 아이콘 언어 사고 — 「도트 이후」 (커뮤니티 피드백 2건)

디렉터 전달 피드백(07-20 스레드): ① UI 도트가 인게임 해상도보다 단계가 적어 "도트일 필요가 있나" + PDA 혼자 리얼컬러로 붕 뜸 + 인게임 모델 복셀 단위 통일 조언, ② "픽셀 모르면 픽셀 하지 마라". 오더 = "도트가 아니라면 어떤 아이콘을 쓸지 사고하라(현 UI 컨셉 = 노스트로모 MU/TH/UR)".

- **진단**: 두 피드백의 공통 결론은 "어설픈 도트 = 마이너스 자산". 텍셀 통일(기존 P1)로도 "손도트 아님" 리스크는 남는다.
- **통찰**: MU/TH/UR 원전 화면에는 도트 아이콘이 없다 — 타이포·세그먼트·라인 스키매틱·기하 픽토그램(론 콥 Semiotic Standard)뿐. UI를 "콘솔 스크린 속 발광 벡터"라는 별도 다이제틱 재질로 규정하면 인게임 픽셀과의 텍셀 비교 자체가 소멸.
- **제안**: 2층 언어 — 1층 텍스트-퍼스트(v1.9.10 탈아이콘 방향 유지·확대) + 2층 세미오틱 글리프(24 뷰박스·2px 스트로크·기하 도형만·단색 currentColor, 색=상태만: 그린/앰버/적). 코드 생성(AI 생성 아님, gen-icons 전례) → 07-16 오더 준수. PDA는 인광 모노크롬 회귀. 상세 = UI-PIXEL-UNITY.md §5.
- **결정 대기**: 글리프안 GO/NO · 잔류 도트(인벤토리 실물 그림) 처리 · 복셀 단위 통일 트랙 개설 여부.
