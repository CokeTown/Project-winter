# 작업 일지 — 2026-07-18 (토)

브랜치: gd-2.0

## 오늘 한 일

### #209 접지·접합 전수 감사 상환 (완결)
- 요트 바닥 y=0.09→0 (전 16셸터 실측, 유일 이단자) · #212 골든 플레이키 완결(등대 빔 renderOrder 실시머 소거+난수 재시드+날씨 이월 리셋+반투명 렌더 골든 숨김)
- 부유/매몰 가구 11종 접지(min.y=0 하네스 실측) · 로케일 src↔public 동기화(게이트 적신호 상환)
- 물 위/타워 새 B안(둘레·링 퍼치 + 3D 도망) · P2/P3 상환 18건(워크플로 트리아지+적대검증). **#209 전량 마감**

### #117 DLC 게이트 + #119 서포터 콘텐츠 (완결 — 6종)
Steamworks DLC 게이트 실작동(platform.js `dlc.owns` — localStorage 오버라이드 + `nineSteam.isDlcInstalled` 브릿지, `STEAM_DLC_APPID` 플레이스홀더). 게이트 3면(제작대 `c.dlc`·툴바 `def.dlc`·복장 크래프트). 콘텐츠 전량 `dlc:'supporter'`·무도면·코스메틱(페이투윈 회피):

| 콘텐츠 | 내용 | 커밋 |
|---|---|---|
| 러시안블루 고양이 | 코트 5번째, 구세이브·입양 소급 그랜트, 마이그레이션 valid list | (앞 세션) |
| 곰인형 | 복셀 플러시 + 빨간 목도리, comfort 0, 고양이 퍼치 0.2 | 92c93b2 |
| 전구 무드등 | 유리병 발광 랜턴(emissive), selfLit/comfort 없음=순수 코스메틱, 고양이 뚜껑 퍼치 0.37 | ddc65a4 |
| 얼룩파카 | 클래식 실루엣 + camo 2톤 얼룩 오버레이(코트 앞면 9패치) | e3829a6 |
| 청바지 | 데님 재킷+청바지 팔레트 | e3829a6 |
| 검정 정장 | style:'suit' — 흰 와이셔츠 + 붉은 넥타이 + 라펠, 목도리 스킵, beanie=머리색 | 0056300 |

- 검증: 실캡처 검수(고양이 코트·곰인형 접지 min.y=0·무드등 글로우+고양이 퍼치·복장 3종 정면/측면 클리핑 0) · 게이트 false→override→true · 배터리 102/102 · 골든 17/17
- **디렉터 몫**: Steamworks DLC AppID 발급 후 `STEAM_DLC_APPID.supporter` 기입 + DLC 가격 책정(콘텐츠 6종으로 상향 근거)

### #199 UI 개편 마감
- PDA·일지 우측 도킹, 본문 카피 이모지 소거(809키 → 모노 아이콘/제거)

### #121 데모 정책 + itch.io EN 빌드 (완결 — 데모 브랜치 4319261)
- 데모 정책: 디렉터 확정 "15로 할거야" → 15일 컷 「보름의 겨울」 유지.
- itch.io EN 변형: 영어 기본 HTML5 웹 빌드. `ITCH_BUILD=1` 플래그가 base `'./'`(itch 해시 경로)·`__ITCH__` EN 기본·SW 주입 off로 전환. 재사용 스크립트 `tools/build-itch.ps1`(가드+web/EN 빌드+zip+정상 dist 복원).
- 근본원인 규명: `opts.lang` 기본값 `'ko'`가 fresh install(nw-opts 없음)에서 항상 truthy → `setLang(opts.lang||autoLang)`의 autoLang이 도달 불가(= #34 자동감지도 잠재 데드). 레버는 opts 기본값 → `state.js`에서 `__ITCH__`일 때 `'en'`. 유저 명시 선택은 nw-opts로 덮음.
- 검증: 오프스크린 부팅 실캡처 — 타이틀 영어(New Game/Load/Survivor's Journal, English 활성)+언어 스위처 유지, 콘솔 에러 0. 정상 데모 경로 배터리 102/102·게이트 green(ko 유지).
- 산출물: `release-itch/nine-winters-demo-itch-en-1.9.5.zip`(149MB). 신규 Windows 데모 빌드=`release-demo/Nine Winters Demo 1.9.5.exe`(현행, 07-17 빌드 — 데모 코드 무변경이라 재빌드 시 동일 바이너리).
- **디렉터 몫**: itch.io 프로젝트 생성 → 위 zip을 HTML(브라우저 플레이)로 업로드, index.html 런치, 뷰포트 1280×720+.

### #210 캡슐 리메이크 + 스크린샷 v4 (완결)
- 방향 전환: "달빛 아래 구도"(디오라마)가 "무슨 게임인지" 안 읽힘 → 레퍼런스=인게임 아이소메트릭 아늑한 방(따뜻한 실내 vs 어두운 밖+눈). 인게임 4K 하네스 직접 캡처라 현재 UI/라이팅 반영.
- 파이프라인: `tools/capsule-shots.cjs`(셸터×시간×세로/가로, store-shot cozy 데코 + 고양이·아바타 고정 + `clearGroundDrops`) + 합성(투명 워드마크, 그라디언트 없음). QA 훅 추가(clearGroundDrops·avatarSys·qaPlaceCat).
- 전 매트릭스 48마스터(8셸터×3시간대×2오리엔트) 촬영 → 콘택트 시트로 디렉터 브라우징.
- **디렉터 픽=옥탑 눈 오는 야간**. 전 포맷 7종 합성 → `docs/steam/capsules-v4/`(vertical 748×896·library 600×900·main 616×353·header 460×215·small 231×87·hero 1920×620·page_bg 1438×810).
- 스크린샷 8종 큐레이션 → `docs/steam/shots/v4/`(셸터×시간 다양성). 커밋 6108ac3.

### 게임 아이콘 교체 (디렉터 지급 엠블럼)
- NINE WINTERS 엠블럼(오두막 씬+워드마크+고양이/빨간 목도리, 1536² 투명)을 게임 아이콘 정본으로.
- build/icon.ico(PNG기반 16~256)·public/icon-512·192·favicon·apple-touch·docs/steam app_icon_184.png 생성(알파 보존). 커밋 58655f0.

### ③ 웹/데모 언어 자동감지 (완결)
- opts.lang 기본 'ko'가 항상 truthy → setLang(opts.lang||autoLang)의 autoLang이 fresh install에서 도달 못 하던 데드패스. GitHub Pages 웹 데모가 브라우저 언어 무관 한국어 고정이던 근원.
- 해결: 세이브·nw-opts 둘 다 없는 최초 실행(_firstLaunch)에만 autoLang(웹=navigator·Steam=클라이언트/OS). __ITCH__=영어 강제 가드. 복귀 유저(loadSave가 opts.lang 반영)·명시 선택 불변.
- 실측: fresh en-US→en·ja-JP→ja·ko-KR→ko / 복귀 nw-opts ko + en-US→ko 존중. 트렁크 98ad6ac·데모 2427f8f, 배터리 102/102.

### ② #147 동부 셸터 4종 (완료 확인)
- 세관·다리 관리소·역 대합실·펜트하우스 4종 다 모델링·라이팅 완비(실캡처 확인 — 펜트하우스 마천루 조망, 역 대합실 빛 웅덩이 나무). 태스크 마커만 오래됨.

### ② #198(g) 엔딩 가중·homeStay 이월 (완료 확인)
- endingLeaning(game.js:7987): escape += 동부겨울×W(1) + 동부9겨울 Wf(3), rest ← settled=max(stayDays, homeStay[current]). 실측: 동부5겨울→escape(8), homeStay40+고양이→rest. 기구현 검증 완료(코드 변경 없음).

### #210 UI 컴포넌트화 — 설정 모달 추출 (Stage 1~2 완결)
- 방향: 디렉터 오더 "소프트웨어 공학적 유지보수·확장 쉬운 방향". game.js(11.5K줄 모놀리스)에서 설정 모달 UI 로직을 `src/ui/settings.js` 팩토리 모듈로 단방향 분리.
- **Stage 1 (1a46740)**: 셸 — 열기/닫기/토글/탭 전환. renderControlsGuide만 ctx 주입.
- **Stage 2 (6d9fd2a)**: 컨트롤 배선 전량 + 탭 기본값 복원.
  - `bindControls(dep)` 2페이즈 패턴: opt-* 리스너(그래픽·사운드·게임플레이·접근성·언어)를 모듈로. bgm/syncBgm/syncSfxAmbience 등 game.js 후반(10307+) 정의를 dep로 늦게 주입 → TDZ 회피. game.js는 syncSfxAmbience 정의 직후 1회 `_settingsUI.bindControls({...})` 호출.
  - `resetTabToDefault`: 탭별 기본값 복원 이관. game.js는 `resetTabToDefault: resetSettingsTabToDefault` destructure 별칭으로 기존 wiring/__shelter export 무변.
  - setSfxVol/setAmbience/setFire는 모듈이 sfx.js 직접 import(안정 함수), 나머지 렌더/오디오 적용자만 주입.
  - game.js 순감 −52줄.
- 검증: 빌드 clean · gates green · 배터리 102/102 · 골든 17/17 · **실 렌더러 기능 12/12**(셸 open/tab/close · opt-colorblind/reducemotion→opts+applyAccessibility body class · opt-quant/bgidle 토글 · 언어 조기반환 · 리셋 기본값복원(pixel 99→3) · **전 배선 이벤트 에러 0**=d 완전 배선·무TDZ·무데드참조).
- **Stage 3 (ac5a610)**: 컨트롤 탭 키 리바인딩 → `src/ui/keybind.js`.
  - `makeKeybindUI(ctx)`: KEYBIND_LABEL·renderControlsGuide·startRebind·captureRebind + 리바인딩 대기상태(awaitingRebind)를 모듈 내부 소유. 입력핸들러는 `isAwaitingRebind()` 게이트로 captureRebind 위임.
  - 입력 코어(KEYBINDS 데이터·keyLabel·save/resetKeybinds·keyForAction·actionForEvent)는 광범위 사용(입력핸들러·__shelter)이라 game.js 잔류 — 모듈은 코어를 ctx로 직접 주입받아 UI만 담당(KEYBINDS/keyLabel 정의 후 생성 → 무TDZ). settings ctx의 renderControlsGuide는 destructure 지연 참조로 무변.
  - 검증: 빌드·gates·배터리 102·골든 17 + **실 렌더러 키바인딩 9/9**(가이드 렌더·PC 12버튼·클릭→대기→키캡처 KeyM→Digit9·localStorage 영속·입력핸들러 게이트·기본값 복원·에러 0) + 설정 회귀 12/12.
- 디렉터 결정: **"Stage 3 키바인딩까지 마저"** → 설정 모달 추출 완결. Stage 4 Electron 디스플레이 IIFE는 플랫폼 글루(추출가치 낮음)라 game.js 잔류. 이후 트레일러 착수.
- **설정 모달 추출 완결**: game.js UI 로직 → ui/settings.js(셸+컨트롤+리셋) + ui/keybind.js(리바인딩). game.js 관련 코드 순감 ~110줄.

### #172 트레일러 V4 신규 스토리보드 재제작 (조립 완료 — 디렉터 시청 대기)
- 디렉터 오더: v25 재활용 아닌 **새 스토리보드로 새로 조립**. Derek Lieu 템플릿 처음부터 적용.
- **v25 그라운딩에서 결함 발견**: 「meeting strangers」 행상 카드가 #200(07-17)에서 소거한 AI 일러 — 07-14 풋티지라 스테일. → V4는 이벤트 카드 컷 대신 파견 UI(c4_send)로 구성해 근원 회피.
- **STORYBOARD-V4.md**(신규 정본): ①장르(가구놓기 0-3s) ②병치훅(죽은도시 유일불빛·무자막 3-12s) ③동사+목표(Decorate·고양이·Scavenge·아홉겨울 12-22s) ④변주 몽타주(무자막 셸터5+한파+미스터리) ⑤페이오프(회색 와이드·로고·위시리스트).
- **자막 디렉터 확정**: 꾸미세요/Decorate · 고양이와 함께/With your cat · 폐허를 수색하고/Scavenge ruins · 아홉 번의 겨울을 버텨/Survive nine winters · 회색은 아름답다. 그리고 그 아름다움만큼 차갑다/The world is grey, beautiful, and bitterly cold. (v25 대비 미니멀·동사 라벨, 목표 명시로 Derek #5 보강).
- 파이프라인 재사용(build-timeline-v4.mjs → assemble4), 기존 60fps 풋티지 재편집(재캡처 0). 산출: `release-trailer-auto/nine-winters-trailer-v4-{en,ko}.mp4`(41.85s·1920×1080·60fps).
- **검증**: 타임라인 math·자막 위치·프레임 유효성 전수(콘택트시트 16컷 + 자막 5종 ko/en + 오프닝·몽타주·버튼 10컷) — AI 카드 부재 확인, 자막 조판(keep-all) 정상.

### #172 트레일러 V5 확장 (2.0 지역 + 시네마틱, 신규 촬영 — 디렉터 시청 대기)
- 디렉터 오더: "2.0 지역 활용 + 컷씬 장면 활용 + **아예 처음부터 재촬영**". V4의 07-14 풋티지 재활용 지적.
- **전 게임플레이 신규 촬영**(현행 빌드): 15클립 재캡처 → tf-v4. 현행 UI·라이팅·정산 패널 반영(구 스테일 UI 소거).
- **2.0 동부 셸터 4종 신규 촬영**: 펜트하우스·역 대합실·세관·다리 관리소(노을 시각). 프레임 검증 — 각 warm 실내+구조 뚜렷, 몽타주용 훌륭. 캡처 스펙 eastern-clips-spec.json.
- **시네마틱 컷씬 편입**(cine-nf 재사용, 디렉터 승인): 죽은도시(병치훅)·금문교 노을(변주·#146 비주얼 쇼크)·폐허 와이드(페이오프).
- 파이프라인: build-timeline-v5.mjs → assemble4(2루트 tf-v4+cine-nf). 산출 `release-trailer-auto/nine-winters-trailer-v5-{en,ko}.mp4`(43.6s).
- **캡처 함정**: trailer-cap2 2번째 리로드 간헐 ERR_FAILED(플레이키) → 1클립/1프로세스로 안정화. 캡처 중 dev서버 탭 조작 금지(크래시).
- **검증**: 콘택트시트 20컷(장르 현행UI·죽은도시 훅·동사+목표·베이스+동부 몽타주·금문교 노을·회색 페이오프·로고) + 동부 4종 프레임. **모션·페이싱·음악싱크는 Fable 판단 불가 → 디렉터 시청**(음악 startAt·컷 타이밍 미세조정 대기).

### #172 트레일러 오프너 리컷 + 펜트하우스 운해 + Director's Cut 발행 (디렉터 피드백 반영)
- **펜트하우스 운해**(디렉터 오더 "높은 설정이니 발밑에 구름, 인게임도 동일"): 1차 블록 복셀 → 디렉터 반려("모델링+좋은 텍스처, 두바이 운해 레퍼런스") → **절차 생성 적운 텍스처(캔버스 메타볼+상단광 볼륨셰이드+방사 알파 4종) + 카메라 향하는 THREE.Sprite 빌보드를 균일 그리드로 촘촘히 겹쳐 연속 운해**. 실 셸터 `buildEnv`에 심어 **인게임 아이소·트레일러 위성 페이오프 공용 1소스**. 죽은 마천루를 구름선 아래로 내려 '운해에서 솟은 스카이라인'. 커밋 `15283e2`. golden 미커버(펜트하우스 비대상)·gates green. 실캡처 검증(위성 t0/t1·인게임 발코니 4앵글).
- **트레일러 오프너 리컷**(커뮤니티 피드백: 초반 3~5s 장르 킥·오소그래픽 꾸미기·앰비언트 모션): `gameplay-motion.cjs CLIP=kick` 신설 — **빈 방 → 착착착 가구 배치(편집 그리드+툴바+가짜 커서, 9종 순차 스냅) → 완성된 코지 룸**. 쾌적도 22→87 상승이 화면에 보임. 몽타주 셸터 6컷은 아바타 숨김 재캡처(#197 침대 지터 회피). 조립 `build-cine-trailer.mjs`(concat 하드컷·BGM 드롭 몽타주 정렬). 산출 `nine-winters-cine-{ko,en}.mp4`(27.08s·ffprobe 검증). **전달 완료**(모션·페이싱은 디렉터 판단).
- **릴리즈**(디렉터: "최신 데모+정식(Director's Cut) github 발행"): **데모=`v1.9.5-demo` 이미 발행됨**(exe 2종+APK, 브랜치 HEAD "1.9.5 마감") → 그대로 최신. **Director's Cut=`v1.9.6-dc` 신규 발행**(gd-2.0 2.0 콘텐츠+운해, 데모 1.9.5 상위로 버전 1.9.6, 커밋 `aeca837`) — NSIS 설치본+포터블 2종. **APK 미빌드**(이 환경 gradle/ANDROID_HOME 부재 — 디렉터 머신/CI 필요, 데모 APK와 동일 경로). 릴리즈 URL: github.com/CokeTown/Project-winter/releases/tag/v1.9.6-dc
- **함정 기록**: (1) 블록 스프라이트 리비전 시 dist 재빌드 필수(하네스가 dist 로드). (2) 기존 `v1.9.0-dc` 릴리즈 존재 → 에셋 이름 충돌 + 에셋 DELETE는 세이프티 클래시파이어 차단 → 파괴적 교체 대신 신 태그(1.9.6). (3) 데모 버전(1.9.5)>본선(1.9.0) 역전 발견 → DC를 1.9.6으로 올려 "최신" 정합.

### #1 봄눈 녹음 + 배속 조정기 + QA 데모 개방 + v1.9.7 양 에디션 발행 (APK 로컬 빌드 포함)
- **#1 봄눈 상호작용**(디렉터 오더 "봄에 눈 내리는 게 땅에 천천히 쌓이고 해 뜨면 녹고"): 물리엔진 아닌 **타깃 상태-심**(조사 결론 — 방치형/배터리/결정성엔 물리엔진 부적합, 지붕눈은 "오류 범벅" 함정이라 지면 평면만). `snowCover` 스칼라를 계절(겨울 0.30/그외 0.10)·`dayness`(낮일수록 목표 하향 + 녹는 속도↑)로 지수 접근. 실측 낮 0.087 vs 밤 0.568.
- **배속 조정기**(디렉터 오더 "2~4배속, 파밍·인게임 시간만, 엔딩 1회 후 해금, QA 치트로 즉시"): SPEED_STEPS[1,2,4] · `speedUnlocked()`(endingSeen‖escape‖qaUsed) · `activeSpeedMult()`를 gmRate에 곱. btn-speed(해금 시 표출·클릭 순환·토스트) + QA 패널 "배속 해금" 버튼. i18n 3언어(btn.speed.title). 수치 검증 완료.
- **QA 데모 개방**(디렉터 "데모도 미발매니 QA 치트"): `DEMO_QA_OPEN=true`로 데모 봉인 우회(`if(DEMO_ED && !DEMO_QA_OPEN) return`).
- **#2 물 반사 — 시도 후 revert(재작업 대기)**: CubeCamera→envMap 반사 베이크 구현, 골든/시각 통과했으나 **오프스크린 렌더 스모크 테스트를 비결정적으로 파괴**(펜트하우스 로드 `matrixWorld null`, 3회 중 2회 실패). 격리 실험(호출 비활성→102/102, revert 후 3회 측정→플레이키)으로 **지붕눈과 동일 계열의 상태-오염 함정** 확인. 지면눈(#1)은 상태-심이라 안전, 반사만 보류. gd-2.0 `a2ee496`·데모 `9c13611`(cherry-pick revert).
- **APK 로컬 빌드 해결**(직전 일지 "APK 미빌드 — gradle/ANDROID_HOME 부재" 상환): 툴체인은 **실재**했음 — JDK21·Android SDK가 **MSIX 샌드박스 LocalCache**에 존재(`...\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Local`). PowerShell(패키지 앱)의 `$env:LOCALAPPDATA\jdk21`(실 Local)은 안 보이고 샌드박스 경로만 접근 가능 → JAVA_HOME을 샌드박스 jdk21로 지정하니 `gradlew assembleRelease` 서명 빌드 성공. `build-demo-apk.ps1`은 이미 양 경로 프로브 로직 보유.
- **발행 2종**(디렉터 "dev 다음 버전 릴리스 + apk 로컬 빌드해 올려"):
  - **[v1.9.7-dc](https://github.com/CokeTown/Project-winter/releases/tag/v1.9.7-dc)** (Director's Cut, gd-2.0 `d1454df`): NSIS 설치본 + 포터블 exe + **APK**(com.projectwinter.shelter, vc20).
  - **[v1.9.7-demo](https://github.com/CokeTown/Project-winter/releases/tag/v1.9.7-demo)** (demo-vertical-slice `4063e6b`): 데모 설치본 + 포터블 + **APK**(`.demo` appId 나란히 설치, vc34). 데모 core 102/102.
- 검증: gd-2.0 core 102/102(반사 revert 후) · 데모 gates green(i18n 2237) + core 102/102 · 양 브랜치 푸시.

## 남은 것
- **#2 물 반사 재작업**: 오프스크린 CubeCamera 상태-오염 해결(렌더 상태 복원 or 실기 전용 게이트) 후 재도입. 봄눈 녹음(#1)·운해는 안착.
- **펜트하우스 렌더 스모크 플레이키**(선재): 클라우드 스프라이트 SwiftShader 비결정(`matrixWorld null` ~간헐). 실기 무관·테스트 하네스 한정이나 배터리 신뢰성 위해 스모크 견고화 후보.
- ③ #210 UI 컴포넌트화: **설정 모달 추출 완결**(Stage 1~3, 위). Stage 4 디스플레이는 플랫폼 글루로 game.js 잔류 결정 · #168 텔레메트리(방식 결정)
- **다음: 트레일러(#172)** — TRAILER-V3-PITCH.md 기반 인게임 풋티지 품질 보증(디렉터 직접 녹화 체제)
- ② #146 금문교 노을 비네트(아트 방향 필요)
- 서포터 고양이 전용 모션은 디렉터 라이브 눈 필요분으로 보류
