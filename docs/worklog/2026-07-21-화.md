# 2026-07-21 (화) 작업 일지

## HUD 기획서 v0.1 상충안 + `ui-hud` 브랜치 1차 구현

디렉터 오더: GPT 작성 「게임 HUD 기획서 및 상세요구사항 v0.1」(docx) 전달 — "여기 나온 사항들과 지금 현 게임의 상충안을 만들어서 별도 브랜치를 통해 제작해봐".

### 상충안 (docs/design/HUD-SPEC-RECON.md)

- 스펙 판독: pandoc 부재 → docx unzip + document.xml `<w:t>` 추출(819문단, 요구 ID 60여 개). 시안 검토(UI-REWORK-REVIEW) 때 기각했던 장르 이탈 지표(승무원·산소·방어등급)가 스펙엔 이미 없음 — 시안보다 성숙한 입력.
- **판정 프레임**: 스펙 승 4(그리드→명령 바 · 색=심각도 · 수치 병기 · 알림 큐) / 게임 승 3(탐험 오프스크린이라 "탐험 모드 HUD" 무의미+적재량 무시스템 · 모바일 제외 선언은 APK 동시 출시와 모순 · 기온/풍속 무시스템).
- **구오더 대체 판정 2건**(디렉터 확인 필요로 문서에 명기): v1.9.10 "색=정체(게이지별 고유색)"·"수치=PDA 전용" → 스펙 8.5/HUD-A02가 대체. 되돌림은 각 1커밋.
- 스펙 자기모순 1건 해석: HUD-E04(아이콘<글자) ↔ 8.4(실루엣 규격) — 의도(P-02 텍스트 우선)로 해석, 아이콘 단독 사용 금지(E05)로 갈음.

### 1차 구현 (`ui-hud`, ui-semiotic에서 분기)

- **E-바**: 좌상단 2×4 그리드(스펙이 명시 제거 대상) → 하단 중앙 명령 바. 주 5(탐험·제작·취침·청소·이주) + 구분선 + 보조 3(지식·옷장·도움, .sec 축소). 버튼 id·배선 불변(이동만). title/ui-hidden/wallpaper 모드 숨김 배선. 팁 쪽지 bottom 22→86px(겹침 회피).
- **게이지 심각도**: `gaugeSev()` 공용(0~25 위험/26~50 주의/51~ 정상, 스펙 임계로 45→50 이동) — HUD `.g-fill`·도킹 LED·PDA `.pbar` 3면 동일 토큰(NFR-004). 색=심각도(그린/앰버/적), 게이지별 고유색 폐지. 수치 병기 복원 + 상태 문자(주의/위험) — 로케일 3언어 6파일 `g.state.warn/crit` 신설(2196키). 점멸 1.3s 주기(≤0.8Hz 스펙 상한, 기존 0.85s 위반이었음).
- **A패널 행 스택**: 게이지 3열 그리드 → 행(라벨|세그먼트 바|수치) — 스펙 6.1 문법. 수치는 게이지 루트에 append(행 순서 확보).
- **검증**: 하네스 캡처 3회전 — 1차에서 도움 버튼 랩 이탈+쪽지/캠버튼 겹침 검거 → 슬림화+쪽지 상향으로 해소. 심각도 강제 캡처(기력 15)로 적색 바+위험 문자+수치 동시 발현 확인(ACC-04). 갈증 40 주입은 오토드링크가 85로 복구 — 시뮬 살아있음의 방증. i18n 2196 무결 · 하드코드 신규 0.

### 남은 것 (2차 이후 — HUD-SPEC-RECON §2)
- Tab 정보 확장 · 알림 큐(동시 2·우선순위·자원 묶음)+시스템 로그 · PDA 사용 중 HUD 축소 · 색상 토큰화 · 1366×768 골든 라인.
- 디렉터 확인 2건: 색=심각도·수치 병기로의 구오더 대체 승인.

## 명령 바 상단 이동 (디렉터 "기존 하단 ui를 화면 상단으로")

- **이동**: `#action-bar` 하단 중앙 → 상단. 뷰포트 중앙 정렬은 1280에서 시계와 71px 겹침(브라우저 rect 실측) → **좌 HUD·우 시계 사이 좌측 고정 밴드**(left:336·max-width:440)로 전환. 시계 좌측 가장자리가 뷰포트 폭과 무관하게 ~783px 고정(줌·레이아웃 앵커)이라 우측 앵커 대신 좌측 고정이 정답.
- **검증(브라우저 rect)**: 1280→바 336–763·HUD끝 328·시계 783 겹침0 / 1920→바 336–763 동일 겹침0 / 390 모바일→상단은 HUD와 세로 겹침 → **모바일은 하단 유지**(엄지 도달성 우위, 디렉터 "상단으로"는 와이드 대상). 일시정지 표시 62px 하강·쪽지 하단 복원.
- **육안(1280 오프스크린)**: 명령 바 상단 밴드 정착, 게이지 심각도(기력 위험 적색) 동시 확인.
- **게이트**: i18n 2196 무결·하드코드 신규 0·배터리 102/102·모달 DOM 7/7.
- **⚠️ 골든 lodge 플레이키(HUD 무관·기존)**: 재고정 직후 check1=0.041%(완벽)인데 check2=lodge 4.6%/8%. **두 체크는 별개 프로세스** — lodge의 옥외 요소(관측소·케이블카 비동기 빌드 또는 반투명 z-정렬)가 오프스크린 SwiftShader 크로스프로세스 순서에 따라 양자 상태로 뒤집힘 = [[#212]] 미해결분과 동종. **HUD 변경 무죄 근거**: diff가 블록 8%의 덩어리 3D 오브젝트(상단 얇은 바 아님) + 직전 두 패스는 우연히 같은 상태로 캡처돼 통과했을 뿐. 관측소/케이블카는 전부 Lambert(불투명)·창 스카이는 MeshBasicMaterial(불투명)·성에는 opacity0 — 가시 반투명 부재라 정착 타이밍 유력. **보고: 16/17 안정 + lodge 1건 기존 플레이키. 원인 규명은 골든 결정론 후속(#212 계열)으로 이관, HUD 커밋을 막지 않음.** 커밋된 baseline은 state A(check1 확인).

## v1.9.12 범프 + 릴리스 발행 (디렉터 "빌드해서 올려봐")

- 수동 범프(bump-version.ps1 UTF-8 함정 회피): package 1.9.12·versionCode 25·PATCHNOTES 「계기판을 다시 짜다」. 3종 빌드(포터블·Setup 각 254MB·electronDist 우회, 서명 APK 147MB) → GitHub Release **v1.9.12-dc** 발행: https://github.com/CokeTown/Project-winter/releases/tag/v1.9.12-dc
- 내용 = ui-hud 브랜치 전량(글리프 솔리드 3차 + HUD 상단 이동 + 게이지 심각도 + PDA 듀오톤). 골든 lodge 플레이키는 릴리스와 무관(런타임 영향 0).

## 문서 통합 + MASTER 인계 체계 신설 (디렉터 오더)

디렉터: "MD 전부 읽고 통합·중복 병합(단 일지는 제외). 마스터 MD를 만들어 세션 종료 시 한 일을 적고, 다음 세션이 항상 먼저 조회해 인계받는 방식 제작."

- **`docs/MASTER.md` 신설** — 인계 정본. §0 세션 시동/마감 절차 · §1 현재 좌표 · **§2 세션 인계 로그(세션말 갱신, 최신이 위)** · §3 문서 지도 · §4 불변 규칙 · §5 역할 분담 · §6 디렉터 대기 결정. 다음 세션은 §1+§2 최신 블록만 읽으면 인계 완료.
- **배선**: `CLAUDE.md` 최상단에 "MASTER를 가장 먼저 읽는다 + 마감 시 갱신" 규약 추가(프로젝트 지침이라 항상 로드됨) · `OPERATIONS.md` §0 시동 절차 1순위를 MASTER로 교체, §6 마감에 MASTER 갱신 추가.
- **중복 병합**: 로드맵 **4→1** (`ROADMAP.md` 구판 · `design/ROADMAP.md` · `design/ROADMAP-RELEASE-2.0.md` → `MILESTONES.md` 흡수 후 삭제, 살아있는 D/F/G 트랙은 "상시 트랙"으로 이관) · 마케팅 **2→1** (`marketing/COMMS-KIT.md` → `MARKETING.md` §10~14) · `HANDOFF.md`(07-12 stale) → MASTER §5 역할 분담으로 흡수·삭제 · 폐기본 2건 삭제(`ENDINGS-REV2` REV3가 대체·근거 보존 / `docs/DESIGN-REVIEW.md` v0.9.1판, design/ 최신판과 파일명 충돌).
- **트레일러 8종**: 정확한 중복이 아니라 **계보**(v1→V2→V3방법론→V4정본)라 삭제하지 않고 한 폴더로 모음 — `marketing/TRAILER-SCRIPT.md` → `steam/trailer/`로 이동(marketing 디렉토리 소멸), `steam/trailer/README.md` 신설로 정본(BRIEF 무드·V3 방법론·**V4 샷 정본**·CUT-SHEET·SCRIPT)과 계보를 표로 분리.
- **L10N은 병합하지 않음** — `l10n/L10N-GUIDE.md`(용어집·시트 규격, steam 문서가 참조 중)와 `design/L10N-JA.md`(일본어 조판)는 역할이 달라 과병합이 해롭다고 판단.
- **현행화**: `PROJECT.md` 07-04자 낡은 수치 실측 교정(game.js 5,700→**11,701줄** · i18n 363→**2,196키 3언어** · 가구 20→**47종** · BGM 36→**37곡** · 모듈 55개), 구 넘버링 절 명시, §11 로드맵은 MILESTONES로 위임.
- **검증**: 삭제·이동 후 내부 링크 전수 스캔 **깨진 링크 0** · MASTER 참조 16개 문서 실재 확인 · 잔여 참조 3건 교정(DESIGN-HISTORY·GD-1.0·ENDINGS-REV3). 문서 수 77→**71**(worklog 제외).

## 디렉토리 구조 재편 (디렉터 "구조도 어느정도 재편 필요하면 해")

- **루트 4GB 격리**: `.gitignore`에 `release-trailer*/`(2.6G)·`release-r/`(1.4G)·`builds/`·`*-verify.txt`·`demo-build-out.txt`·`Pixel_Fixed_Capsule/` 추가. **untracked 15건 → 2건**(잔여 2건은 실제 아트 소스라 유지). 매 세션 `git status`가 오염되던 문제 해소.
- **`docs/qa/` 해소** — 성격이 다른 3종이 섞여 있었다: 감사 `AUDIT-1.4` → `reports/AUDIT-1.4-2026-07-05.md`(날짜 접미사로 타 리포트와 규약 통일) · 게이트 계약 `RELEASE-CYCLE` → **`docs/RELEASE-CYCLE.md`로 승격**(OPERATIONS와 동급 프로세스 문서) · QA 세이브 픽스처 4종+1 → **`tests/fixtures/qa-saves/`**(코드 픽스처가 docs에 있던 오배치, 참조 0 확인 후 이동·BALANCE-NOTES 경로 갱신).
- **OPERATIONS ↔ RELEASE-CYCLE 역할 명시**: 전자=**어떻게 실행하나**(절차·명령), 후자=**무엇이 통과해야 하나**(12게이트 계약). 상호 링크로 관계 고정 — 병합하지 않은 이유를 문서에 남김.
- **`docs/marketing/` 해소**(직전 배치 연속): 문구→MARKETING §10~14, 트레일러 스크립트→`steam/trailer/`.
- **`docs/localization/`** 빈 껍데기(추적 0) 제거 — `l10n/`과 이름만 다른 유령 디렉토리.
- **`docs/design/`(32개)는 평면 유지 + 색인 신설**: 하위 폴더 분할은 MASTER·일지·메모리·타 문서의 경로 참조를 대량으로 깨뜨리는데 얻는 건 탐색 편의뿐이라 손익 불일치로 **기각**. 대신 `design/README.md`가 6분류(서사/시스템/UI·아트/리뷰·이력/기술·규약) 색인 담당. 오배치 1건만 이동: `GROUNDING-AUDIT-2026-07-17` → `reports/`(날짜 스냅샷은 설계 정본이 아님).
- **검증**: 이동·삭제 후 문서 링크 전수 재스캔 **깨진 링크 0** · docs 하위 디렉토리 8→6(qa·marketing·localization 소멸).

## docs 심화 재편 — design 평면 33개 → 5개 주제 폴더 (디렉터 "docs 쪽을 특히나")

앞 배치에서 링크 손상 우려로 **미뤘던** design 분할을, 경로 재계산을 자동화해 안전하게 완수.

- **자동 링크 재작성**: `scratchpad/reorg-docs.mjs` — 이동 매핑을 먼저 정의하고, 추적 중인 전 `.md`의 마크다운 링크를 **옛 트리 기준으로 해석 → 새 트리 기준 상대경로로 재계산**해 치환한 뒤 `git mv`. 파일 자신이 이동하는 경우도 새 위치 기준으로 계산(`newSelf`)해 `../` 깊이가 자동으로 맞는다.
- **분류 (32개 이동)**: `canon/`(8 — 서사·세계관) · `systems/`(7 — 게임 시스템) · `ui/`(7 — UI·아트) · `review/`(4 — 리뷰·이력) · `eng/`(5 — 기술·규약). 별건으로 `STRATEGY-NEXTFEST`는 스토어 트랙이라 `steam/`으로 분가.
- **함정 2건**: ①`git ls-files`가 한글 파일명(일지 `2026-07-16-목.md`)을 따옴표+8진 이스케이프로 뭉개 크래시 → `-z` NUL 구분으로 교정 ②Windows에 bash 부재로 `mkdir -p` 실패 → Node `mkdirSync` 사용. 두 크래시 모두 **git mv 이전 단계**라 트리 무손상, 링크 재작성은 멱등이라 재실행 안전.
- **잔존 수기 교정**: MASTER 문서 지도의 옛 평면 목록(백틱 참조 7건 + 트리 17줄)은 마크다운 링크가 아니라 자동 치환 밖 → 수기 갱신. design/README 헤더의 "평면 유지" 결정문도 재편 결과로 교체.
- **검증**: 이동 후 **깨진 링크 0**(전 추적 md 스캔) · 코드/도구의 옛 design 경로 참조 **0**(js/cjs/mjs/ps1/json/html/css 스캔) · docs 하위 디렉토리 구조 확인.
- **미착수(의도)**: `docs/steam/` 하위 캡슐·스크린샷 폴더 20여 개는 바이너리 에셋 묶음이라 재편 이득보다 스토어 제출 문서 참조 손상 위험이 크다고 판단 — 그대로 둠.

## 리팩터 가이드 문서화 + MASTER 진척도 축 (디렉터 오더 2건)

**① 리팩터 개선점 문서화** — `docs/design/eng/REFACTOR-GUIDE.md` 신설.
- **실측 기준선 표**를 먼저 박음(감상이 아니라 수치로 진척을 재게): game.js 11,701줄·422함수(전체 src의 44%) · 모듈레벨 let 91 · `state.` 직접접근 1,002 · innerHTML 50 · `_golden` 관통 20 · BAL 209 vs 하드코드 후보 25 · 최장 함수 processDay 488/openCraftModal 444/resolveExpedition 429. 각 지표에 **측정 명령**을 병기해 다음 세션이 같은 방식으로 재측정 가능.
- **"이미 잘 된 축" 3종을 패턴으로 명명**해 복제 대상으로 삼음: `icon()`=Facade(글리프 전환 시 호출부 261곳 대신 1곳) · `i18n.pick()`=Interception Point(로케일 261건 무수정 치환) · `gaugeSev()`=Single Source of Truth(3면 흩어진 임계 수렴). 교훈 = **변경 비용은 관문(seam) 유무가 결정한다**.
- **P1~P5 처방**(증상→측정→패턴→처방→검증→위험): P1 렌더 컨텍스트 객체(Parameter Object+Strategy — 플래그 인자·시간적 결합 제거, **미해결 lodge 플레이키의 근원 치료 후보**라 유일한 '정확성' 항목이므로 1순위) · P2 매직넘버 외부화 · P3 God Object 긴 함수 파이프라인화 · P4 쓰기 통로 지정(전면 캡슐화는 과설계라 명시 배제) · P5 뷰 빌더 seam(반복 3회↑만).
- **과설계 방지 절** 추가: 프레임워크 도입 금지 · 클래스를 위한 클래스 금지("병목은 객체 부재가 아니라 경계 부재") · 전면 재작성 금지(26,555줄에 실화면으로만 검증된 암묵지가 박혀 있고 골든/배터리가 그걸 지킨다) · 테스트 없는 리팩터 금지.

**② MASTER 인계 로그에 진척도 축 추가** — 디렉터: "이전 세션에 어느정도 작업됐는지 서술해 다음 작업 시 맥락을 잃지 않게".
- §2에 **블록 작성 규약**(코드블록 템플릿) 신설 — 필드 순서 고정: 트랙 진척도 / 한 일 / 산출 / 검증 / 다음 한 수 / 열린 결정 / 알려진 이슈.
- **`트랙 진척도`를 첫 필드로** 승격: 트랙별로 전→후를 함께 적는다(예 "UI 리워크: 글리프 100% → HUD 1차 완료, 2차 0%"). 이유를 문서에 명시 — "무엇을 했나"만 있으면 다음 세션이 *전체 중 어디쯤인지* 몰라 같은 조사를 반복한다.
- 기존 3개 블록(07-19·20·21)에 진척도 **소급 기입** — 07-19 터미널 스킨 100%(아이콘은 도트 잔존) → 07-20 글리프 100% → 07-21 HUD 1차 완료·2차 0%로 트랙 연속성이 한눈에 읽히게.
- 배선: design/README·MASTER 문서지도·WORKLINE에 REFACTOR-GUIDE 등재, MASTER §1 "큰 줄기"에 리팩터 트랙(대기) 추가.
