# 2026-07-23 (목) 작업 일지

> 세션 범위: 07-22 밤 ~ 07-23. 관측 단말 마감 라운드(#218 곡률~#220 HUD 배치) + 3언어 조판 결함 3건 연쇄 검거 + 판단물 FHD 체계.

## #218 관측 오버레이 곡률 정합 + 프레임 돌출 (e69159b)

디렉터: "관측 회선·판 UI도 화면에 부착된 거라 같은 곡률로 휘어져야지. Frame 튀어나오는 거 다 잡아."

- 오버레이 전체(`#obs-screen`)에 crtBulgeB — 헤더·패널·핀이 씬과 한 곡률. 패널 개별 볼록은 obs에서 해제(**CRT는 화면에 하나, 이중 곡률 금지**).
- bulgeRetarget obs 분기: 옵션 무관 k=0.045 고정·화면 bbox 기준 — 곡률 아래 리스트 행 클릭을 역변위 [8,-3]px 지점 주입으로 실증.
- back 버튼 프레임 돌출 진범 = 모달 X 규격(`.p-min` width:20px)이 글자 라벨("전체 지도")을 밀어냄 → obs 전용 width:auto+nowrap.

## #219 준비물 행 1줄화 — 카피·코스트·폭 3겹 (f95aabb)

디렉터: "레이아웃 깎을 때 영어도 같이 검수. 들고가는 물품이 2줄인데 레이아웃을 망친다 — 문장을 압축한다던가."

- **카피 압축 3언어**: 성공률 중복 삭제("공업/슬럼 +5%p")·응급키트 "깊은 상처 완화"·가방 "빈손 방지"(이후 #220에서 "최소 1개 보장"으로 재개명). **물병 카피 결함 검거**: "부상 회복 -20%"는 실효과가 요양시간 ×0.8(빨리 회복)인데 페널티로 오독되는 방향 — "회복 가속"/"quick heal"로 교정.
- **en 검수**: Water Bottle→Bottle · First-Aid Kit→Medkit(행 폭 주범이 긴 이름) · "Ind/slum +5%p" 터미널 약칭.
- **행 코스트 costIcons**(아이콘+×n) — 전체 명칭은 '예상 소비' 라인·부족 토스트가 담당(B-④ 원칙의 잉여 표면 방어).
- **패널 260→300px**: 오더 "1/4 가량"(1280의 23.4%) 환원 — 260이 en/ja 행을 꺾던 원인. 결과: **3언어 × 7행 전부 1줄 실측**(2회전 — 1차에서 en Medkit 58px 등 잔존 검거).

## #219 후속 — 렌더 순서 결함 + 라인박스 (7f624dd·285aac9)

디렉터: "한국어 폰트 제외 줄이 안 맞는데 왜이래" → 실측 결과 **폰트 문제가 아니라 렌더 순서 문제**.

- **빈 껍데기 검거**: `icon_prep_*`는 에셋이 없는 이름 — 첫 렌더는 img 404→빈 텍스트, **이후 렌더는 `_iconMissing` 캐시가 빈 `px-icon` span(31px inline-block)을 반환**해 옆 텍스트 베이스라인을 6px 밀었다. 언어 무관·두 번째 렌더부터 발병(ko가 멀쩡해 보인 건 캡처 순서 착시). 폴백 공란이면 아무것도 반환하지 않게 수정 — prep 6·theme 4 등 에셋 없는 표면의 유령 여백 동시 소거.
- **잔여 0.5px = 폰트 혼재**: 둥근모 커버리지를 비트맵 대조로 확정(한글·라틴·숫자·중점 O / **가나·한자 X** — 폭 비교는 CJK 전각이라 판별 불가, 폴백 스택 교체 렌더 픽셀 비교로 규명). ja는 시스템 고딕이 그려 ascent가 달랐다 → `.prep-row > span { line-height: 14px }` 라인박스 고정. 3언어 소수점까지 동일(box 16.4/h22·text 18.4). **Silver 전환 불요 확정**(라틴 전용이라 문제 해소 불가) — 둥근모 유지 존치.

## ja 픽셀 폰트 PixelMplus12 (A안, 디렉터 승인 — e9fe0ce)

- 후보 4종(PixelMplus/Galmuri11/Ark Pixel/Misaki) 제시 → 디렉터 SD 워키토키 제안 → **산돌 조사 결과 기각 사유 3건**(구독 라이선스 임베딩 제외·일본어 0자·비픽셀) 보고 → A안 승인.
- M+ FONT LICENSE(상용·재배포 자유) 원문 동봉. TTF 1,245KB→**WOFF2 276KB**(wawoff2 devDep). `unicode-range`(가나·한자·CJK 구두점)로만 걸어 **ko/en 렌더 무변경을 구조로 보장**.
- 검증 함정 2건 박제: ①캔버스 ctx.font은 unicode-range를 CSS와 다르게 처리(위양성) ②폭·높이는 CJK 전각+라인박스=스택 첫 폰트라 판별력 0 → **실화면 3벌 렌더 픽셀 대조**(스택 vs PixelMplus 100% 일치, vs 고딕 75.6%). 세로 오프셋 보정 없으면 76% 위음성(`probe-jafont.cjs`에 dy 탐색 포함).

## 판단물 FHD 체계 (3bc1b09)

디렉터: "스크린샷 잘 안보이는데 FHD로는 촬영 못하나."

- `aerial-capture.cjs` 기본 해상도 1280×720→**1920×1080**(CAP_W/H 하향 가능). `gallery-shots.cjs` 신설: 전체컷=FHD JPEG q92(CRT 노이즈로 PNG는 컷당 2.8MB — 페이지에 못 싣는다) + 디테일컷=1:1 무손실 PNG 크롭, capturePage 1회에서 동시 취득(2회 찍으면 CRT 위상 어긋남).
- 갤러리 개편: 클릭=1:1 원본 전개, ko/en/ja 패널 무손실 크롭. URL 고정 유지.

## #220 관측·HUD 마감 배치 (b43d9f3)

디렉터 오더 5건+질문 1건: ①관측 회선 헤더 곡률 안쪽(top 10→30/left 14→38 — 곡률 변위 최대 ~15px+여유) ②뷰 컨트롤 우측 끝(right 62→10) ③관측 종료=패널 하단 상시 「나가기」(상단 닫기 폐지, sticky+margin-top:auto, obs.exit 3언어) ④가방 「가방」+「최소 1개 보장」 ⑤명령 바 확대(아이콘 24→30·min-width 48→58)+**「배치 끝내기」 신설**(전체 수거 왼쪽 — 편집 모드 안에서 나가는 길이 없던 실결함) ⑥티어링 질문 답=의도된 지터(강도 노브 존재 고지).

- 골든 17씬 재베이스라인(HUD 시각 변경 — 전량 커밋 규칙) → 2연속 19/19 0%.
- 모달 골든 3건(craft·knowledge·map) 갱신 — **오늘 변경 아님**: #213 이모지 소거분 미갱신 잔재(diff로 📦🔥🌱 확인).
- 배터리 가방 테스트를 리터럴("가방을 꿰맨다")→로케일 키 기반으로 갱신.

## 검증 요약

배터리 102/102 · i18n 2,223 무결(신규 obs.exit·inv.editDone) · check-emoji 무관용 · 골든 19/19(재베이스 후 0%) · 모달 골든 7/7 · 관측/HUD/편집 전 항목 하네스 실측+FHD 실캡처 육안 · 프로세스 스윕 0.

## ⚠️ 이월·함정

- 골든 1회성 플레이키(단일 씬 3~4%대, 재실행 시 소멸) — 기존 #212 크로스프로세스 계열, 2연속 그린으로 판정하는 관행 유지.
- PixelMplus woff2 276KB가 PWA 프리캐시에 포함(오프라인 일본어 보장 비용) — 제외 원하면 globPatterns 조정.
- 다음 트랙: **터미널 전면화**(모달 셸·PDA·토스트에 설정창 문법 + 리프레시 스캔 애니 전 UI 확산 — 디렉터 07-23 GO).
