# 2026-07-23 (목) · 2부 — PDA 확대 + 언어 오버라이드 수복 + 웹 잔재 전수 차단 + #146 결선 + P2 + 재수렴

> 1부(심야 자율 큐 마감)와 별도 세션. 디렉터 실기 신고 → 근원 수리 → 오더 5연타(트리거·타임랩스·P2·#198 설명·재수렴).

## 신고 대응

### PDA 앱 모드 폭 재설계 (5d0bfa7)
- 신고: 제작 탭 이름이 Craft 버튼에 먹히고 재료 안 보임 · 옷장 동일 · 셸터 안내문 좁아터짐 · "PDA 2 더 키워"
- 원인: 좁은 유리에 nowrap 강제 + **축소 우선순위 부재** — 이름·재료가 함께 깎이고 overflow:hidden이 잘라냄
  (실측: Forest Parka 66→54px · 재료 40→33px · 스와치 28→14px · 탭 "De" · 셸터 줄당 8.7자)
- 수리: 하우징 88→96vh(세로 비율상 물리 상한) + **앱 모드 한정 유리 여백 환원**(#211 여백은 탭·게이지용 — 앱 모드엔 둘 다 없음)
  = LCD 폭 431→496px. 행 규칙: 재료·버튼·스와치 고정, 이름만 basis 0 말줄임(auto면 '줄바꿈 먼저'라 버튼 낙하 — 실측).
  옷장은 미보유 안내문이 길어 이름 최소폭 4.5em + 안내문 줄바꿈.
- 검증: en/ko × 1920·1366 전 행 clipped 0 · 셸터 줄당 8.7→35자 · 배터리 102 · 골든 19 · 모달 7

### 컨테이너 뚜껑 (0a73412, 1부 마감분)

## 언어/번역 계열

### 번역 패키지 재추출 + 상시 도구 (93dc979)
- "언어 바꿀 때마다 파일 갱신되나?" → 아니오, 스냅샷. #191 때 생성기를 안 남겨 2,068키에 잠복(현행 2,227 — 159키 어긋남)
- `tools/export-l10n-pack.mjs` 신설: full·chunks·delta·sheet·CHANGES 한 명령. **델타**(신규 163+변경 378=541키)만 돌리면 됨
- 기준선 `.baseline-ko.json` 분리 — full/ko를 기준 삼으면 첫 실행이 덮어 둘째 실행 델타 증발(실측). `--set-baseline`은 역수입 후에만
- 미번역 판정 = "원문 그대로 복사"만(한글 잔존 휴리스틱은 opt.lang 병기·빈 키 오탐)

### 유저 편집 번역이 Electron에서 즉시 원복되던 결함 (b22bdc8)
- 장치는 이미 있었다(resources/locales 편집 → 재시작 반영, README 동봉) — **실증하니 죽어 있었다**
- 인과(탐침 3단): 부팅 병합·표시 성공(x1 직후 실측) → 웹 폴백 fetch('locales/ko.json')가 **file://에서 200 성공** →
  asar 내장 기본 번역 재병합 → 유저 편집 원복. ko 기본값끼리는 무증상이라 잠복
- 수리: Electron 브릿지(window.nineLocale) 존재 시 웹 폴백 스킵. 프로브 4/4(수정 전 1/4 재현)
- 신설: **%APPDATA%\Nine Winters\locales** 1순위 오버라이드(자동 생성) — 포터블은 resources가 %TEMP%에 풀려 편집 증발.
  부분 파일 지원. README.txt 갱신(구 "ko/en만" 낡은 문구 교정)

## 웹 잔재 전수 차단 (f66ea12) — "웹을 Electron으로 싼 티 검증해"
- 실구멍 5: ①기본 메뉴 활성(Alt 메뉴바·Ctrl+R·Ctrl+Shift+I·**Ctrl+W 즉시닫힘**·Ctrl+± 줌·F11) ②파일 드롭=게임 이탈
  ③핀치/Ctrl+휠 페이지 줌 ④가운데클릭 오토스크롤 ⑤타이틀 'Project Shelter' 잔재
- 이미 막힌 것: user-select · img user-drag · 캔버스 우클릭 · 네이티브 confirm
- 함정 회피: will-navigate는 **같은 URL만 허용** — location.reload() 6곳(언어 전환·타이틀 복귀)이 생명줄
- 검증 = **출고 코드 그 자체**: NW_QA_HIDDEN(무플래시·무음 실앱 부팅) 신설 + probe-webisms가 CDP(렌더러)+인스펙터(메인)로
  7채널 계측 7/7. 개발 실행(SHELTER_DEV_URL)은 메뉴 유지

## 오더 5연타

### #146 「불타는 해협」 지역 결선 (910102e)
- 다리 보이는 맵 = **다리 관리소**(세관 아님 — 창밖 주역이 무너진 현수교)
- 노을 창 16.5~20시: 글린트 펄스+호버 커서, 다리 클릭(단일 — 디렉터 원문) → 비네트. 골든 동결 중 숨김
- bridgeSight 히트 평면을 셸터 데이터로(발코니 balcony 문법). SHELTER_HASH 재핀. 프로브 5/5

### 컷씬 24h 타임랩스 ×2 (70ba0be + 전달)
- 1차 아이소 부감판 기각(디렉터 정정: "비네트 컷씬 구도에서 지나는 24시간")
- 러너와 동일한 오버레이 캔버스를 캡처 도구가 페이지 안에 재현, update(t)를 시각→t 매핑(kn 역산)으로 구동
  (게임 코드는 buildJungleSunScene 노출 1줄) — gate=t 확장 구간(심야)까지, jungle=[0,1] 클램프(색 외삽 방지)
- goldengate_cine_24h.mp4 · penthouse_cine_24h.mp4 전달(각 24초 1080p30, 밤→여명→주간 앰버→노을)
- 함정: P2 브랜치 배터리가 dist를 노출 없는 빌드로 덮어 jungle 1·2차 실패 — 재빌드로 해소

### 리팩터 P2 (02f9603 → 병합 8d2fc9f)
- 개조 비용 17종 전량 → BAL.modCosts(기계 추출·치환) + 쾌적 기본 안정감 단일 출처(BAL.comfort.baseSecurity —
  comfort.js 점수식 18과 표시 '+18'이 별개 리터럴이던 구조 봉합)
- **함정 박제: BAL에 comfort 섹션이 이미 있었다** — 앞쪽 신설분이 중복 키(마지막 승리)로 통째 덮여 undefined.
  diff-0 게이트가 병합 전 검거 → 기존 섹션 합류
- 무손실: HEAD 리터럴 vs BAL 전수 diff-0 · 배터리 102 · 모달 7 · check-hardcode 19→10 재고정(잔여=휴리스틱 노이즈)

### 데모 재수렴 (5f93958 + 골든 재핀 ced0618)
- map-aerial → gd-2.0(충돌 0) → demo-vertical-slice(충돌 3계열+골든 17장)
- 로케일 6파일 = **키 단위 3-way**(데모 고유 44키 보존 · 양쪽 변경 0 · 2271키) · game.js = 임포트 유니온 +
  골든 숨김 트렁크 P1 채택(maybeRunDemoCredits 보존) · main.cjs = 유니온 라인
- 게이트 전그린: DEMO_BUILD=1 배터리 91/91 · i18n 무결 · 골든 재핀 후 2연속 0% · 모달 7/7

## 빌드
- release/Nine Winters 1.9.12.exe + Setup (17:48) — PDA·뚜껑·언어·웹 잔재 전부 동봉

## 다음
- 〔D〕 캡슐 v6·스크린샷 v5 검수 · PDA/배치 촬영 소스 · 아이콘 생성본 · depot·GA4·페스티벌 · #198 판정 2건(rate·도시명) · 델타 541키 Gemini
- 〔F〕 #198(g) 엔딩 가중·homeStay 이월 · 컷씬 타임랩스 재구도 요청 대응
