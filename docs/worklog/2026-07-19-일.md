# 작업 일지 — 2026-07-19 (일)

브랜치: gd-2.0 · 전 세션 개발 서버(vite 8420) 실화면 그라운딩

## 물 반사 재작업 + 펜트하우스 크래시 근원 (07-18 밤 연속)
- 지난 "물 반사가 스모크 깬다" **오진 정정**: 스택 캡처로 진범 규명 — `THREE.Sprite.raycast`가 `raycaster.camera`를 요구하는데 **아바타 장애물 검사 레이(카메라 미설정)**가 운해 스프라이트를 훑어 `matrixWorld null`. 잎 클러스터와 동일 함정, 신규 운해 스프라이트에 미적용이 근원. **실기 크래시**(테스트 한정 아님).
- 처방: 운해 `raycast=noop`. 플레이키 1/8 → **0/16** 근절. 물 반사 재도입(try/finally 경화). 커밋 `be59043`, 데모 cherry-pick `0d17083`. 메모 [[sprite-raycast-camera-gotcha]].

## PC 디렉터판 PDA·UI 전면 검수 배치 (디렉터 실시간 rapid-fire)
- **아이콘 상단 점**: 파이프라인 잔여 밴드(y0~2) 110개 안전 제거(글리프 상단 42개 보존). `6e24ef9`
- **PDA 뒤로 → 홈**: 앱 모드(좌측 메뉴 진입)면 앱만 닫고 PDA 홈(상태 탭)으로 → 인벤 조회 확보. `6e24ef9`
- **PDA 폰트 큼·제작 클리핑**: 앱 모달 11→9px·h2·버튼·행 압축 → 제작 레시피 단일행 정렬. 상태탭 11→9.5px. `6e24ef9`·`7a3d5bb`
- **지역 탐험 HUD 패널 삭제**: display:none, 진입=상단 메뉴 탐험 버튼. `7a3d5bb`
- **탐험 중 PDA 빈 화면**: 상태 탭에 진행률 카드(지역·%) + 부상 치료 버튼 이관. `7a3d5bb`
- **염료 페인트통**: 색 점 → 회색 통+흐르는 도료색 SVG(12계열 식별). `77c0fec`
- **붉은 섹션 헤더 축소·화살표 제거**: 인라인 12px 헤더를 스코프 `!important` 8px, 제작 행 `← ` 아이콘 제거(Bold 이름만으로 결과물 판독). 상태탭 폰트 4pt 추가 축소(7.5px).
- **제작 상위 카테고리 탭바** (디렉터: 「3번째 사진처럼 상위 버튼 → 선택」): 긴 단일 스크롤 → `가구·물품 / 복장 / 공사(개조+프로젝트) / 꾸미기` 4탭. 전 카테고리를 DOM에 렌더하고 비활성은 CSS `display:none`으로만 숨겨 **탭 전환 즉시(재렌더 0)** + 도면 게이트 검사(전 레시피 DOM 존재)와 양립. PDA 탭바 톤·상단 sticky. i18n `craft.catBuild`·`catDeco` 3언어 신설(2194키).
- 전부 개발 서버 실화면 검증(4탭 전환·박스 미초과·무전 기지 노출). core 102/102 유지.

## 지도 신고 2건 (디렉터: 수산시장 겹침 + PDA 지도 마킹)
- **수산시장 라벨 겹침**: 근원 규정 = 라벨이 ~33% 폭이라 하단 밀집대에서 좌·우 핀 이름이 중앙에서 마주쳐 겹침(항만 야적장↔수산시장, +연구동·검문소). `MAP_MARKERS` 재배치 — 항구 2종은 남서 해안 **세로 적층**(둘 다 이름 오른쪽), 봉쇄 3종은 동편 **세로 적층**(이름 왼쪽). 야적장 라벨과 겹치던 cabin 셸터 점도 상향(29,66→34,60).
  - **실측 검증**: `window.__shelter`로 전 지역(11)+전 셸터(12) 노출시켜 픽셀 BBox 전수 대조 → **라벨-라벨 0건·라벨-셸터 0건**. 실화면 스크린샷 확인.
- **PDA 미니맵 마킹**: 지도 탭(`.pmap`)에 해금 지역을 GPS 점으로 마킹(`pregion` — 방문 전 흐린 회록·다녀오면 밝은 라임). 디렉터 후속: 「내 거점 위치만·셸터 명칭 텍스트 불필요·공간 절약」 → 셸터명 노트 제거, 내 거점은 `pyou` 점으로만, 이름은 title 툴팁. 텍스트 노트 0.
- modal-golden `map`·`craft` 재기준화(의도된 UI 변경). 타 모달 4종은 기존 드리프트(무관)라 미변경. core 102/102·gates 그린.

## 이모지→아이콘: 선별 원칙 확립 + 가구 누락분만 메꾸기 (디렉터)
- **원칙 전환**: 「인게임 모든 이모지에 아이콘 붙이지 마라 — 텍스트 옆 장식성은 오히려 AI 느낌. Symbolism 필요한 것만 선별」(디렉터). 무한 ♾️=무한모드 전용 불필요, 구멍 🕳️=용도 마이너 → 둘 다 드랍.
- **17종 렌더 지점 감사**: 대부분(🎒업적토스트·🪨프로젝트명·🎭🪣🌈전리품줄·❌실패제목·🥽🔫이름옆·🎣노트·★숙련텍스트·🕳️이벤트제목·🧤단열재)이 **인라인 텍스트 옆** → 전부 이모지 유지. 셸터개조(SHELTER_MODS)도 `${m.emoji}` 인라인이라 제외.
- **진짜 심볼 슬롯 = 가구 아이콘 누락분**: `furnIcon()`(독립 `<img>` 슬롯)로 렌더되는 가구 47종 중 **아이콘 PNG 부재로 색 이모지 폴백** 중인 2종 색출 — **곰인형 🧸(teddybear)·전구 무드등 🏮(moodlantern)**. 나머지 45종은 단색 아이콘인데 이 2종만 컬러 이모지로 튀던 불균질(=AI 티) 봉합.
- **소스**: MIT 픽셀 라이브러리 조사(pixelarticons 877·HackerNoon 248, 스크래치패드). 무드등=pixelarticons `lightbulb`(MIT, 벤더링), 곰인형=자작 24그리드(라이브러리 부재). 단색 렌더러 `mono-raster.mjs` 파서 버그(z 이중 소비→서브패스 병합) 수정.
- **배포·검증**: `#e6efe4` 단색 192×192로 `public/img/icons`+`assets-src/art/out/icons` 배치, `icon-mono-gen.mjs` MAP 등재(재현성). dev 8420 실브라우저에서 5종(bed·sofa·lamp+신규 2) 전부 192px 정상 로드 + 실화면 스트립 톤 일치 확인. 게임 코드·i18n 무변경.

## 영어/이모지 감사 + 코지·탐험 신고 (디렉터 rapid-fire 2차)
- **영어 튜토리얼 json 유출**: 근원=quest-text/lore가 textContent 렌더 → en/ja 번역에 박힌 `<img>`가 글자로 유출(ko는 순수 텍스트라 무증상). 감사(4축 워크플로) 결과 실유출은 이 2키뿐(HTML 키 201개 중 나머진 innerHTML 경로라 정상). 싱크 innerHTML + ko 아이콘 대칭화. `114fd5d`
- **이모지→아이콘 전수 감사**: 26종 매칭 아이콘 존재, 17종 무대응(빗물통·가방·네온·젤·낚시·총·삭제·실패·숙련★·무한 등). 디렉터 결정 = **전부 도트 나올 때까지 대기**(07-16 신규 AI 생성 금지). 감사 계획 scratchpad 보존.
- **코지 탐험 이벤트 22%→11%** + **무너진 입구 보상 다양화**(도료 50%→30%, 자원 잡동사니 2종→4종): 「다 보라 도료」 단조 완화. `114fd5d`·`bdffefe`
- **탐험 진행중 UI 복원**: "지역 탐험 삭제" 때 exp-panel 인라인 display:none이 진행률 바까지 가림 → 인라인 제거 + 상태 토글(.show)로 탐험 중·부상 중만 노출. `f58a476`

## 라이팅 대폭 개선 (디렉터: 시간 흐름 불분명·따뜻함·실제 태양)
- **골든아워 온기**: dawn/dusk 회색 fog·hemi를 앰버로, 지평선 채도↑, 태양광↑. 낮/밤은 핵겨울 냉기 유지. `a7e6591`
- **실제 태양 궤도**: 지향광을 24h 대원 궤도로 굴려 해가 동→서 이동 → **그림자 방향이 하루 종일 회전**(정오=짧음·해뜰녘=동측 긴 그림자·황혼=서측 반대 방향). 실내는 고정. `e0034b9`
- **24시간 하늘 팔레트**(디렉터 「NUCLEAR WINTER 24-HOUR SKY」 반영): 4단→7단 확장 — 밤→새벽(청)→해뜰녘(금)→정오(크림)→오후(베이지)→황혼(붉음)→땅거미(보라)→밤. `d337248`
- 전부 실화면 그라운딩(시각별 캡처 대조). core 102/102 상시.

## 무전 기지 셸터 개조 (디렉터: 무선기지국 설치 가능 집엔 개조 기능)
- 개척 프로젝트 「무전 기지 복구」 완공(`radioBaseDone`)과 연동한 셸터 개조 `radiostation`: `SHELTER_MODS` + `modAvailable` 플래그 게이트(`m.gate`) 신설 + `buildRadioStationProp`(지붕 마스트·접시·십자 다이폴·붉은 항공등 selfLit). 지하(subway) 제외.
- 실렌더: 컨테이너 지붕 안테나 설치 확인. core 102/102·i18n 무결. `37dd9bc`
- 디렉터 확정 방향(완공 연동). 대안(개조=건설 수단·특정 집·송출 기능)은 요청 시 조정.

## 디렉터 신고: 뒷문 + QA 절단기
- **뒷문 여는거 "왜 없어"**: 규정 결과 **구현돼 있음**(벙커 제작창 「🔩 뒷문 개방」 → `bunkerBackdoor` → `clearPassage` → `bunkerUndercroftRoute` 금지 구역 지름길). 미노출 원인 = 게이트(**돔 벙커 거주 + 절단기**). 실기 렌더 확인.
- **QA "자원 전종 +100"에 절단기 없음**: 절단기는 자원이 아니라 `hasCutter` 플래그(드랍템)라 자원 루프가 못 줌 → 벙커 뒷문이 QA에서도 안 열림. res100 + QA 에디션 상주에 **절단기·총 지급 추가**(책은 자원이라 이미 포함). `ea578e2`

## 릴리스
- **v1.9.8 (dev / Director's Cut)** — 버전 범프 `f92b207`, exe+APK GitHub 발행(디렉터 검수용).

## UI 픽셀 통일 대패스 (디렉터 rapid-fire — DCInside `aidevelop/1111` 피드백 기반)
동인 = 커뮤 정밀 피드백 "AI가 문제가 아니라 관점(테마 미스매치) 문제 — 표준 픽셀 사이즈·색상수·도트폰트 통일" + Loop Hero/노스트로모 레퍼런스. 지배 스펙 문서화=`docs/design/UI-PIXEL-UNITY.md`(6대 규율·정본 규격·5카테고리 감사).
- **PDA 픽셀화 opts 연동**(디렉터 커뮤 확정 아이디어): 게임 픽셀 설정(`opts.pixel`)에 PDA 하우징 텍스처 연동 — 같은 배수+부스트(**옵션보다 +2 강하게**)로 다운샘플·액센트 보존 K40 양자화한 프리베이크 픽셀본(`{pda04,dock_pda}_px3~8`, 색 10k→~36색) 스왑. `pdaTexUrl()`/`applyPdaTex()`(applyOpts 훅), quant off/pixel≤1→원본. **라이브 검증**: pixel3→px5·6→px8·1/quantoff→원본 전 케이스. 도크는 53px 표시맞춤 px8+pixelated.
- **아이콘 텍셀 통일 1차**: 토큰 스케일 `--icon-sm/md/lg/xl`(16/20/24/28) + 기본 클래스 em→px(맥락 독립). HUD/PDA 밀집 개별 px는 nowrap·그리드 피팅이라 2차 보존.
- **웹폰트 누수 봉합**: `#radio-bubble` 폴백 통일(미로드 Galmuri11 제거) · 패널 최소화 `□`/en-dash→ASCII `+`/`-`.
- **즉시 3건**: `#quest .q-art` 스무스→pixelated · 1.5px 서브픽셀 보더 2곳→정수 · `sw-cnt` radius 7px→토큰.
- **i18n 누수 봉합(디렉터 신고 "언어 바꿔도 Log에 한국어")**: `dayLog.notes`·`winterMemoir`가 `t()` **해석 문자열**을 세이브에 저장 → 언어 전환(reload) 시 옛 언어로 굳음. 언어 스탬프(`dayLog.lang`/`page.lang`) + 부팅 시 언어≠현재면 노트 비움/pending 드롭. 라이브 검증. 교훈=노트는 키+인자 저장, t() 결과 저장 금지.
- **이모지 스트립**: 프로젝트/제작 모달 헤더 16곳(🛖🔩🏗️…) · 시간대 밤아이콘(맵 라이팅+시계 대체) · 슬롯 shelter/season 이모지. (쾌적로그 아이콘은 이미 렌더 안 되던 죽은 데이터)
- **난이도 = 슬롯카드 테두리색**(디렉터 확정 방향 실현): 🔥💀♾️🖼️ 배지 폐지 → `.slot-card[data-mode]` 테두리색(코지 초록·생존 앰버·혹한 적색·무한 청록·꾸미기 보라). 5색 라이브 검증.
- **아이템 이모지 → icon() 배선(class B, 13곳)**: 가구=`furnIcon()` 즉시 픽셀 업그레이드(47종 실재), 이벤트·개조·프로젝트·테마·준비물은 `icon(키,이모지)` 배선(PNG 부재→이모지 폴백, 디렉터가 `icon_ev_/mod_/proj_/theme_/prep_*` PNG 제작 시 자동 반영). 전리품은 이미 icon() HTML이라 제외. 색상 칩(벽지/바닥)은 색 유지.
- **검증**: 빌드 다수 그린 · gates 그린(i18n 2194·하드코딩 신규 0) · 브라우저 MCP **DOM/JS 계층 살아있어**(페인트만 타임아웃) `javascript_tool`로 부팅·연동·색상 실게임 검증.

## 노스트로모/MU-TH-UR 터미널 UI 실적용 + 3종 빌드 (디렉터 "적용시켜보자" → "좋다" 확정)
UI-PIXEL-UNITY 규율 6(내러티브 UI) 실현 — 웜 차콜 패널을 녹색 인광 터미널로(PDA 언어를 전 UI 크롬으로 확장). 커밋 `d65dc2f`(style.css).
- **패널 프레임**: 얇은 녹색 보더(rgba 140,245,166,.30) + 근흑 인광 바탕 + 스캔라인 + 곡률 비네트, 헤더 상단 코너 브래킷(구 리벳 자리 재활용).
- **디지털 세그먼트 게이지**(디렉터 요청 — mockup 디지털 느낌 합치기): `#hud .g-bar` 9px + `::after` 반복 그라디언트로 칸칸이 분절 + 인광 글로우 + warn 앰버/crit 적색 깜빡임(dkblink).
- **터미널 헤더**: `.p-head` `//` 섹션 프리픽스 + 점멸 상태 LED(ledBlink, reduce-motion 소거) + 인광 타이틀 + 녹색 섹션 라인. 버튼 `button.pixel-btn` 얇은 녹색 인광 박스.
- **영어 기본 폰트 +1px**(디렉터 "네모 안에서"): `html[lang="en"]` --fs 토큰(10/12/15→11/13/16) + 버튼 라벨(10→11). 게이지 라벨(.g-name)은 3열 그리드 오버플로 위험으로 제외.
- **검증**: 라이브(javascript_tool DOM/JS 채널) — 게이지 세그먼트·LED 색(rgb 140,245,166)·`//` 프리픽스·헤더 보더·부팅 전부 설계대로. 빌드·게이트 그린(i18n 2194).
- **빌드 3종**(디렉터 "싹 다 빌드"): exe/setup가 EPERM(`win-unpacked.tmp→final` rename — Defender 실시간검사가 갓 추출된 electron.exe 핸들 점유) 2회 실패 → **`-c.electronDist=build/electron-dist`로 추출 단계 우회**(데모 빌드 관례) 해결. 산출: `Nine Winters Setup 1.9.8.exe`(NSIS 253MB)·`Nine Winters 1.9.8.exe`(포터블 253MB) signtool 서명 + retail APK(cap sync → gradlew assembleRelease, JDK21 주입·keystore 서명, `Nine-Winters-1.9.8.apk` 146.6MB). 3종 `release/` 집결. **디렉터 결정=1.9.9 범프+새 릴리스** → v1.9.9 범프(versionCode 22) 후 GitHub Release **`v1.9.9-dc`** 발행(3종 서명본, https://github.com/CokeTown/Project-winter/releases/tag/v1.9.9-dc). ⚠️ 범프 함정: `bump-version.ps1`의 `Set-Content`(PS5.1 ANSI)가 PATCHNOTES·build.gradle 한국어를 mojibake 손상 → git 복원 + Edit 재적용으로 봉합(메모 [[workline-and-release-rules]]).

## 남은 것
- 데모 브랜치에 PDA UI 배치·무전 기지·QA 수정 포팅(현재 gd-2.0만) — 데모 재발행 시.
- 무전 기지 개조 세부(방향 대안)·프로젝트 카드 레이아웃 실기 재확인.
- **UI 통일 2차**: 밀집 아이콘 px 수렴(실화면) · radius 광역 정규화 · class B 아이콘 PNG 제작 · 시그니처 화면 데코. (노스트로모 터미널 UI 실적용은 완료 — 위 섹션.)
