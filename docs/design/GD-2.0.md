# GD-2.0 — 「응답」 (스케치)

> **한 줄**: 1.4에서 허공에 보낸 송출에 **응답이 돌아온다.** 낙진이 걷힌 세계의 심장부로 들어가 진실을 마주하고, 아홉 번째 겨울에 자신의 자리를 선택한다.
>
> 상태: **설계 완결** (스케치 → 구현 계획까지, 디렉터↔CTO 2026-07-08). 착수: 정식판 후반 — **2.0 = 발매 1~2개월 후 최종 업데이트(진짜 엔딩)**.
> 출시 로드맵: **1.0 발매 → 1.1~1.4 월 1회 업데이트 → 2.0「응답」 최종 업데이트.** (커스터마이징·가구 추가는 로드맵과 별개로 상시 병행.)
> 관련: [[STRATEGY-NEXTFEST.md]] §1(아치) · [[DEPTH-DESIGN.md]] §10(채택 3종) · [[WORLDVIEW.md]](캐논 §1·§3 개정 반영). **구현 계획은 §9.**

---

## 1. 왜 "응답"인가 — 1.4가 끝처럼 보이는 문제의 해소

베타 테스터가 1.4를 결말로 느낀다 = 설계대로 읽은 것(1.4는 "엔드게임" 라벨 + 주제 회수까지 완료).
2.0의 위험 = 사족으로 보이는 것. 해법은 레트콘 없이 1.4의 마지막 비트를 **소급해서 문으로** 만드는 것:

- 1.4 결말: 무전 기지 복구 → 수집 방송 송출 → 지도에 생존자 불빛 점등. *(현재는 여기서 '답'으로 끝남)*
- **2.0 개막: 그 송출에 응답이 돌아온다.** 지도의 불빛들 = 이제 갈 수 있는 곳. "끝인 줄 알았던 것이 초대장이었다."
- 마케팅 포지션: 콘텐츠 확장이 아니라 **"이야기를 끝내는 업데이트"**. 1.x = 세계의 완성, 2.0 = 서사의 완성.

## 2. 낙진 시계 — 시간이 세계를 연다 (디렉터 확정)

핵겨울 낙진은 2~3년이면 상당 부분 가라앉는다(현실 근거 있는 밴드). 이를 게임 시계에 박는다:

- **1~3번째 겨울**: 금지 구역(1.4)은 방호복으로만. 도심 중심지는 진입 불가(치사 선량).
- **3번째 겨울 이후**: 낙진이 걷혀 **금지 구역이 맨몸 개방**되고, 그 너머 **도심 중심지(신규 최상위 지역)**가 열린다.
- **방호복 재프레임** (가치 붕괴 방지): 방호복 = "먼저 들어가는 자의 어드밴티지". 1~3겨울에 선진입 하이리스크 루팅
  vs 3겨울 후 안전 진입의 후발 — 투자 시점이 결정이 된다. (지역 숙련②와 시너지: 먼저 들어간 자는 숙련도 먼저 쌓는다.)
- 깊이 정의와의 정합: "같은 지도를 다시 마주쳤을 때 더 보인다" — 시간이 지도 자체를 바꾼다.

## 3. 도심 중심지 — 하이 밸류, 하이 텐션 (디렉터 시나리오)

- **루팅**: 하이 밸류 물건(희귀 부품·의약 정점·사치 가구·책 뭉치)의 최상위 소스. 후반 경제의 새 천장.
- **밴딧 (약탈자 무리)**: 낙진이 걷히자 사람들이 심장부로 몰린다 — 그리고 "함께 살지 않는 이유"가 실증된다(§2 저류의 물리화).
  - 수위 규칙: **오프스크린/간접 우선** — 통행세 쪽지, 경고 사격 *소리*, 털린 은닉처, 모닥불 연기. 대면은 희귀 + "찰나의 긴장 접촉" 층위(회피/거래/손실의 결정, 온스크린 전투 시스템 없음).
- **변종 (방사능 변이체)**: 판데믹 병원체가 방사능 환경에서 변이한 무언가 — 도심 깊은 곳에서 *목격되는* 존재.
  - 수위 규칙: **크리처 호러 금지. 스토커의 존(Zone) 문법** — 소리, 그림자, 스쳐 지나감, 흔적. "봤지만 싸우지 않았다."
  - 서사 기능: 공포 장치가 아니라 **연구실(§4)로 이끄는 빵부스러기**.
- **적대 존재 모드 다이얼 (디렉터 확정 2026-07-08)**:

  | 모드 | 적대 존재의 실체 |
  |---|---|
  | 노말 | **소리만 낸다** — 존재는 앰비언스와 소문. 습격·조우·손실 없음 |
  | 하드 | **마주친다** — 조우 시 파밍 실패 가능성 발생 (전리품 손실/빈손 귀환) |
  | 하드코어 | **총이 없으면 탐사 중 중상을 입을 수 있다** (사망 아님 — "사망 없음, 탈진만" 정체성 유지. 디렉터 확정 2026-07-08). **중상 = 부상 서사화(④)의 정점 티어**: 의약품 소요 급증·회복 장기화·memoir 흉터. 총 = 내구성제(소모), 연구실에서 파밍, 수리 가능(코스트 비쌈). 발사는 오프스크린 판정 — 전투가 아니라 로드아웃·정비의 결정 (DEPTH-DESIGN §10.2) |

## 4. 진실 — 연구실과 다음 인류 (디렉터 시나리오)

도심 중심지의 심부에 **연구실**이 있다. **그들의 정체와 의도 (디렉터 확정 2026-07-08)**:

- 소각을 결정한 이들 중 일부는 **상호 확증 파괴를 멈추고**, 새로운 세계를 지으러 떠났다 — 그곳의 잔존 인류를 모아 다시 신세계를 만들려 한다.
- 단, 그것은 우리가 바라는 신세계(모든 인류가 하나 되어 재건·화합·규합)가 아니라 — **기술을 가진 자신들의 주도로 설계한 신세계 질서**다.
- **그들은 살아남는 자를 지켜봤다. 이곳에서 버텨낸 주인공은 그들의 눈에 '합격'했다.** — 이 사실은 연구소와 봉쇄 구역들의 메모로만 발견되도록 제작한다 (조각 순서: 관찰 기록 → 선별 기준 → 규합 계획 → 초대).
- 기존 스파인과의 접합: 박사(소각 반대표, rsc9)와 궤도 관측소는 이 네트워크의 마지막 관측자. 변종은 이 진실의 그림자.
- lore.js rsc 라인의 종착지: "소각은 끝이 아니라 이관(移管)이었다" — 세계관의 답(#35)의 최종 층. 신세계 엔딩의 양가성(§5)은 여기서 나온다: 인류의 재건이라는 도약 / 거절할 수 없는 초대라는 순응.

## 5. 엔딩 3분기 — 재정의 (디렉터 언어로 갱신)

구 정의(연결/초월/영원회귀)를 디렉터 시나리오로 **구체화**. 정서 매핑은 유지, 그릇이 선명해졌다:

| 엔딩 | 무엇 | 시점 | 구 매핑 |
|---|---|---|---|
| **탈출** | 진실을 다 보기 전에 박사의 연락을 받고 구조선에 오른다 — 떠나는 자의 엔딩 | **조기 가능** (9겨울 전, 박사 스파인 진행 시) | 연결의 원형 (기존 구조 엔딩 흡수) |
| **신세계** | 연구실의 진실을 받아들이고 다음 인류의 질서를 향해 나아간다 | 아홉 번째 겨울 | 연결의 어두운 구체화 |
| **안식** | 진실을 알고도 — 오늘의 고양이, 이 집, 나의 겨울을 위해 다시 숨는다 | 아홉 번째 겨울 | 초월+영원회귀 통합 (디렉터 철학의 코어) |
| **(히든) 침묵** *(가칭)* | §5.1 히든 루트의 끝 — 사일로의 버튼. 그러고 끝 | 히든 루트 완주 시 | 4번째 축: 거절마저 거절 |

- **신세계의 온도 (디렉터 확정 2026-07-08)**: "다시 인간들의 세상"을 만들기 위한 **도약**임과 *동시에*, 거절할 수 없는 **"섬뜩한 순응"**. 희망과 공포를 둘 다 — 어느 한쪽으로 해석을 닫지 않는다. lore 저작은 이 양가성을 유지할 것.
- **Day 10000** = 어느 엔딩이든 그 후의 먼 에필로그(관측 위성 다리 서사) — 계속 플레이 보상으로 존속.
- 엔딩은 "누적된 하루"가 빚는다: 어디서 겨울을 보냈나(도시), 무엇을 주웠나(진실 조각), 무엇을 지었나(집) — 갑툭튀 선택지 금지(STRATEGY §1.2).

### 5.1 히든 루트 — 올라가는 곳이 없는 지하철 (디렉터 확정 2026-07-08, 진짜 이스터에그)

발견 자체가 콘텐츠인 4번째 길. **어떤 UI 힌트도 없다** — 커뮤니티가 서로에게 속삭이며 발견하는 종류의 것.

1. **입구**: 지하철 선로 복구(1.2)를 **끝까지** 완성하면 노선의 끝에 연결 지점이 생긴다 — *올라가는 곳이 없는* 역(1.4 구현 그대로 활용 — 이미 시각적으로 출구가 없다). 승강장 어딘가의 **특정 지점을 터치/클릭**하면 특수 통로가 열린다 (숨겨진 인터랙션 — 눌리지만 아무 표시가 없다).
2. **개척**: 통로 너머는 **막대한 자원**을 들여 개척해야 한다(대형 프로젝트 최고 코스트). 완주하면 **2.0 지도로 이어지는 네 번째 문** — 정규 3수단(지하철·케이블카·예인선)과 별개의 비밀 경로.
3. **연구소와 유보**: 이 길로 연구소에 닿아 **박사와 상호작용**한다. 떠나지 않고 남은 채로 — 선택을 **유보**한다.
4. **박사의 문서**: 유보한 자만이 발견한다 — 박사가 남긴 문서. **그들이 어디로 갔는지, 그리고 미사일이 어디로 갈 수 있는지**가 적혀 있다. 그들의 의중(§4 — 선별·규합·자기들의 질서)을 이미 메모로 읽어온 플레이어에게, 이 문서는 좌표가 된다.
5. **사일로**: **미사일 사일로**. 버튼을 누르면, 그들이 간 곳으로 핵이 터진다. **그러고 끝.**

- 톤: 설명도 정당화도 없다. 상호 확증 파괴를 *멈춘* 자들을, 그 파괴로 끝내는 플레이어 — 게임은 판단하지 않고 끝난다. (침묵이라는 가칭은 여기서 온다.)
- **구현 함의 (디렉터 확정 2026-07-08)**:
  - **지하철 역사 연출**: 1.4 모델 그대로 쓰되 **조명을 확 줄인다** — 버려진 역이므로 어둡게, 남은 조명은 **붉은 비상등만**. (조명값 변경이라 골든 스크린샷 갱신 필요 — 신규 지오는 아님)
  - **계단/사다리는 개척 *후*에 나타난다**: 히든 통로를 제작·개척한 뒤 기존 역사에 계단(또는 사다리) 모델이 추가되어 "연결되어 있다"를 시사한다. 발견 전에는 아무 단서도 없다.
  - 히든 터치 포인트: 시각 변화 없는 순수 히트 영역(골든 게이트 무충돌). 실수 방지로 더블 터치/홀드 요구 검토.
  - 사일로 시퀀스: 신규 아트 최소로 — 어둠·계기판·박사의 문서(종이 연출 재사용)·버튼·화이트아웃이면 충분하다.

## 6. 도시와 이동 — 확장 4부작의 문을 연다

이동 수단은 1.x가 이미 지었다. 2.0은 그 문을 여는 것:

| 문 (1.x 기존) | 수단 | 닿는 곳 | 톤 |
|---|---|---|---|
| 1.2 선로 복구 | 🚇 지하철 | **도심 중심지** (§3~4 — 수도의 심장) | 진실·긴장. 계절 무시(캐논) |
| 1.3 케이블카·스키 | ⛷️ 산맥 | **관측 마을** (천문대·온천·고요) | 안식 성향. 겨울 유리 |
| 1.1 예인선 | ⛵ 해안 | **항구 도시** (크레인과 컨테이너의 부재의 박물관) | 발견·넓이. 여름만(결빙) |
| 귀환 | 어느 길로든 | **첫 도시** | 안식 엔딩의 그릇 |

- **9겨울 배분(확정)**: 1~2겨울 = 시작 도시 고정(정 붙이기 — 떠남의 무게는 정든 다음에만 성립) → 이후 자유 순서 → **아홉 번째 겨울, 어디에 있든 구조가 온다.** 체류지가 엔딩 성향에 가중치, 최종 선택은 플레이어. 안 떠난 자의 9겨울도 유효(안식 루트).
- **이주 리듬**: "한 겨울, 한 도시". 이주 = 대형 프로젝트(경로 개척+짐 — 1.1 시스템 재사용). 왕복 가능하되 비싸게.
- **지역 숙련(②)과의 맞물림**: 숙련은 지역에 쌓인다 → 이주 = 쌓은 숙련을 두고 가는 것. "떠남의 정서적 무게"가 연출 없이 메커니즘에서 나온다.
- 계절 창 × 대한파 프론트(③): "언제 떠나느냐"가 결정이 된다.

## 6.5 전파망 — 「응답」의 문자적 구현 (디렉터 2026-07-08)

§1의 "응답"은 지금 **스크립트**(먼 불빛·박사)다. 디렉터 제안 = 그 응답 층에 **진짜 다른 플레이어**를 얹는다: 혼자 보낸 전파가 낯선 생존자에게 닿고, 그의 전파가 내 라디오에 뜬다 — *"누군가가 ○○라고 보냈던데, 수신양호."* "둘러싸였으나 혼자"([[WORLDVIEW.md]])의 은유를 메커니즘으로 직역한 것 — **이름값(응답)을 하는 기능.** 기믹이 아니라 2.0의 테제. (정본화: docs/design/REWARD-LOOP.md의 "완성·수집" pull과도 접합 — 수신 자체가 수집 대상이 될 수 있다.)

**형태 — 비동기 유리병 편지 (실시간 멀티 아님)**:
- 플레이어가 전파 송출 → **익명 풀에 POST**. 접속/특정 조건에서 풀의 전파 1개 GET → 라디오 인카운터로 수신. "수신양호/불량"의 라디오 텍스처로 노출을 큐레이션.
- 계정·PII 없음. **오프라인이면 기존 스크립트 풀로 폴백**(코지 오프라인 정체성 사수).

**안전 설계 — 조합형(권장), 자유입력 아님**:
- 유저 텍스트를 낯선 타인에게 = 공개 콘텐츠 → 검열 책임 발생. **큐레이션 문구 조각(무전 코드북)에서 조립** → 출력 공간 유한 → **런타임 검열 불필요** + 단파 무전 톤([#82 방송 전문 단문화])과 정합. 자유입력은 욕설필터·신고·수동검수 부담이라 코지에 과함 → **비채택.**

**2단계 롤아웃 (비용 의식 — 디렉터 "또 다른 코스트는 싫다")**:
- **1단계 (1.x, 백엔드 0)**: 라디오 생존자 풀에 **큐레이션된 실제 전파를 씨앗으로 프리베이크**(베타/데모 폼 수집 또는 저작). 인프라·런타임 검열 0, 감정 페이오프의 8할. `survivorLights`/`distantLight`/`tryRadioBroadcast` 파이프라인 ♻️ 통째 재사용.
- **2단계 (2.0 라이브 릴레이)**: 서버리스(Cloudflare Workers + KV급) async 릴레이 — 익명 POST/GET. 백엔드+검열 = **의도된 비용 결정.** 오프라인-우선 아키텍처를 처음 깨는 지점 → 착수 전 별도 승인.

**정체성 정합**: 수신 = 따뜻한 흔적("바깥에 나 말고도 버티는 누군가")→ 세계관 희망 층. 위협 층은 §3 적대(밴딧·변종)가 전담. **관계 발전·채팅 금지**(접촉 스펙트럼 "찰나의 접촉" 유지).

**상태**: 방향 정본화(디렉터 2026-07-08). **코드 미착수** — 1단계는 1.x에서 저비용 파일럿 가능, 2단계(라이브)는 비용·검열 결정 후.

## 7. 캐논 개정 — ✅ 반영 완료 (디렉터 승인 2026-07-08)

WORLDVIEW 개정 1차로 반영됨 (각 항목에 게임 문법 번역 병기):

1. **접촉 스펙트럼 "적대적 존재" 층위** + 모드별 게임 문법 표(노말 소리만/하드 조우·파밍 실패/하드코어 중상) — WORLDVIEW §3.
2. **플롯에 낙진의 시계 + 심부의 연구실 스파인** — WORLDVIEW §1.

## 8. 열린 질문 (다음 논의)

- ~~신세계 엔딩의 도덕적 온도~~ → **확정** (§5: 도약이자 섬뜩한 순응, 양가성 유지).
- ~~핵의 표적~~ → **확정** (§4~§5.1: 신세계 세력의 본체. 박사의 문서가 좌표를 준다. 함의 = 자기들 주도 질서에 대한 최종 거부이자, 소각의 반복 — 게임은 판단하지 않는다).
- ~~하드코어 탐사 사망 처리~~ → **확정** (중상 — 사망 아님. 의약품 소요 급증, 부상 서사화의 정점 티어).
- 변종의 정체 확정: 판데믹 병원체의 방사능 변이(제안) vs 연구실 실험의 그림자 — lore 저작 전 결정 필요.
- **사일로 도달 경로**: 히든 루트 전용인가(제안 — 유보한 자만 발견), 정규 루트에서도 조각 단서가 보이는가.
- 도시별 셸터·지역 볼륨(도시당 셸터 2~3 + 지역 3 제안) 및 아트 예산.
- 밴딧 "통행세/거래" 메커니즘의 구체 형태 — 암시장(1.2)과의 관계.
- **전파망(§6.5) 검열 모델**: 조합형(문구 코드북) 확정 vs 자유입력 — 조합형 제안(런타임 검열 회피).
- **전파망 라이브 백엔드 결정**: 1단계 프리베이크 유지 vs 2단계 서버리스 릴레이 착수 — 비용·모더레이션 게이트(디렉터 "코스트 최소" 선호 → 1단계 우선).

---

## 9. 구현 계획 (엔지니어링 스펙 — 코드베이스 접지, 2026-07-08)

> DEPTH-DESIGN §9(지식 트리)와 동급의 착지 계획. 원칙: **기존 시스템 최대 재사용(♻️) · 신규 최소(🆕) · 오프스크린·사망 없음 캐논 사수 · sim 결정론/세이브 스키마 불변 우선.**
> 각 블록 = 대체로 "데이터 추가 + 판정 훅 1~2개". 착수 순서는 §9.9. (모든 파일:함수·상태필드는 현행 코드 실측.)

### 9.1 낙진 시계 + 도심 중심지 (지역·시간 게이트)

- ♻️ **재사용**: `state.winters`(넘긴 겨울 수 — `passWinter` 증가·세이브 마이그레이션 완비) · `seasonIndex`/`SEASON_DAYS`(core/season.js) · `regionUnlocked`(core/regions.js — 지역 해금 단일 판정 지점) · REGIONS 스키마 + `v.id=k` 주입 루프 + `rateParts`/`rollRes` 탐험 판정 파이프라인 · `openMapModal` 마커 렌더(스케치/잉크·MAP_SAFE 자동 클램프) · 방호복 내구 게이트(`BAL.forbidden`).
- 🆕 **신규**: (1) 낙진 시계 게이트 — `regionUnlocked`에 `state.winters >= 3` 분기(현재 `state.successes` 전용 → winters import 필요) + `startExpedition` 방호복 차단을 winters≥3에 우회하는 '맨몸 개방' 분기. (2) 도심 중심지 = REGIONS 신규 엔트리(`citycore` 가칭 — 최상위 lootRes + 고위험 injuries) + `MAP_MARKERS` 좌표 1 + `MAP.regions` 좌표 1 + 게이트 분기 + `regionIcon` 아이콘 에셋.
- 📁 **데이터**: world.js REGIONS +1 · game.js MAP_MARKERS·MAP.regions 각 +1 · balance.js 게이트 상수(`BAL.forbidden.barehandWintersGate` 등).
- 🚦 **게이트**: 코어45 REGIONS 해시 갱신 · 신규 마커 MAP_SAFE 프리뷰 정합(#111 선례) · sim: 겨울 게이트는 day 파생(결정론 유지) · **후반 게이트라 초·중반 밴드 불가침**.
- ⚠️ **리스크**: '맨몸 개방'은 적/전투가 아니라 **방사능 내구·부상 확률 상향**으로만. successes×winters 이중 게이트 정책 단일화. 최상위 lootRes → 후반 인플레(luxury.surplusCap) 시뮬 재캘리브.

### 9.2 적대 존재 다이얼 (밴딧·변종)

- ♻️ **재사용**: `isHard`/`isHardcore`/`rescueEligible`(core/mode.js) · `BAL.encounters.freqMul` per-mode 다이얼(적대 강도 선례) · `applyInjury`+INJURIES · gloves/firstaid 완화 문법 · `exp.bag` 최소회수 안전망 + `notes[]` 인과문(손실 서사화) · 오프스크린 3채널 전부(`dropMemo`/`tryDropMemoOnExpedition` · `tryRadioBroadcast` · 밤하늘/먼불빛) + pending-popup.
- 🆕 **신규**: `resolveExpedition`(또는 `departExpedition` 직후) **조우 롤 훅** `hostileEncounterRoll(region, mode)` — 노말=무판정(소문만) / 하드=실패 시 rollRes 전리품 몰수·감산 / 하드코어=총 미보유 시 중상 확정. 노말 전용 밴딧·변종 '목격·흔적' 소문 인카운터(events.js `when.districts`+mode) + 소문 memo/방송 서브테이블.
- 📁 **데이터**: balance.js `BAL.hostile`(per-mode rollChance·lootLossFactor·severeInjuryChance) · lore.js 적대 소문 · events.js 목격 인카운터.
- 🚦 **게이트**: ⚠️ **sim 결정론 최대 지점** — `resolveExpedition` RNG 스트림은 시드 시뮬 기준선(Day30 clear544/storm435)의 load-bearing. 조우 롤 `Math.random()`은 반드시 `!_simRunning` 가드 또는 시뮬 기대값 모델(game.js 시뮬 블록) 동시 확장. 모달: 중상·빈손 결과.
- ⚠️ **리스크**: 온스크린 전투 금지(회피/거래/손실 판정층에만) · 중상≠사망 · drawEvent 조우와 파밍-조우 롤 이중화 시 빈도 폭증 방지 · 노말 '소리만·손실 0' 계약(소문에 기계적 스테이크 0).
  - **✅ 구현 확정 (2026-07-08)**: `resolveExpedition` 조우 블록 — citycore 전용 + `!_simRunning` 이중 가드, 트립당 `BAL.hostile.encounterChance`(0.35). 노말/무한=소리·흔적 노트 2종(손실 0) / 하드=이번 트립 전리품 ×0.5 몰수(가방 최소회수 안전망은 조우 **뒤**에 적용 — "챙기면 빈손 없음" 계약 유지) / 하드코어=총 1발 격퇴(손실 0) 또는 무총 시 중상+몰수. 조우 중상 뒤 실패-부상 롤이 경상으로 덮어쓰는 다운그레이드 가드. 소문 인카운터·lore 서브테이블은 §9.5 lore 저작과 함께(후속). 검증: 코어 57종(적대 3모드 계약 + Day30/60·하드코어 사망 핀 불변 = sim 스트림 무접점 입증).

### 9.3 총 + 하드코어 중상 (로드아웃·정비)

- ♻️ **재사용**: **방호복 내구 패턴 통째 복제** — `state.hazmat={dur}` + `craftHazmat`/`repairHazmat`/`wearHazmat` 3종 + `gateCost` 모드 스케일 + `BAL.forbidden` 수치. CRAFTS 레시피 스키마 · PREPS 로드아웃 슬롯 · `lab` 파밍 테이블 · INJURIES apply/treat/tick + 감염 악화 패턴.
- 🆕 **신규**: `state.gun={dur}` + `wearGun()`(도심 중심지 탐험 시 dur−1, wearHazmat 미러) + 하드코어 게이트. INJURIES에 `critical`(중상) 정점 티어(높은 pen·장기 restH·cure=의약품 대량) + **다단계 악화**(minor→deep→critical; 현행은 →infection 1단계뿐) + `state.scars[]` 흉터.
- 📁 **데이터**: items.js RESOURCES+gun / CRAFTS+총 레시피 / INJURIES+critical · balance.js `combat` 섹션 · i18n gun.*/injury.*.
- 🚦 **게이트**: save.js 마이그레이션(gun·scars 기본값) → **tests/core.test.cjs MIG_HASH 스냅샷 갱신 필수** · sim 결정론(내구·중상 판정) · 골든 무영향(온스크린 없음).
- ⚠️ **리스크**: 발사=오프스크린 판정만(전투 시스템 금지) · 하드코어 전용이라 노말/하드 밴드 불변 · 총 게이트가 '없으면 탐사 봉쇄'급 벽 안 되게 파밍/수리 캘리브 · **도심 중심지(§9.1) 선행 필요**.
  - **✅ 구현 확정 (2026-07-08)**: `state.gun={dur}` — 제작 없음, citycore 성공 탐험 파밍 드랍(하드코어 전용·미보유 시 30%, 절단기 문법) + 제작대 정비 행(방호복 미러, `gateCost` 스케일, 격퇴 1회=1발·만충 6발). INJURIES `critical` 신설(pen 0.35·restH 72·timeMult 1.5·의약품 대량). **다단계 악화**: 기존 방치-악화 롤의 목적지만 모드 분기(신규 RNG 없음 — 스트림 불변) — 하드코어 minor→deep→critical / 노말·하드 기존 →infection. 흉터는 §9.4-④ scars가 자동 수확. MIG_HASH 재핀(-2113534371 — gun·scars·front 필드 스냅샷 편입). 잔여(후속): prep 모달 citycore 위험 안내 라인 · 총 파밍/수리 코스트 실측 캘리브(§9.5 사이클에서).

### 9.4 깊이 3종 (지역 숙련 · 대한파 프론트 · 부상 서사화)

- **② 지역 숙련** — ♻️ `state.regionVisits`(이미 증가·마이그레이션 안전) + `rateParts` 합산 아키텍처(know()/comfort() 게터처럼 항 1줄). 🆕 `masteryBonus(regionId)` 게터(visits→성공률%p + 전리품 질) + 티어 임계 + `BAL.mastery`(**cap 필수**). ⚠️ 상한 없으면 후반 0.95 천장 조기 도달 · pickAutoRegion `revisitDecay`와 튜닝.
- **③ 대한파 프론트** — ♻️ 한파 예보→발동→지속→종료 파이프라인 전체 + severity/방어단계(core/coldsnap.js) + `winterSnap.acc` 누적 + `winterPrepAdvice` + 눈사태(1.3)가 이미 같은 예보 문법 재사용(검증된 확장). 🆕 연례 격상 이벤트(severity 2~3·긴 리드·겨울당 1회 확정) + **자기 규율 선택 모달**(배급 반/잠 줄이기/비상식량 — Frostpunk 법령의 1인칭. **고양이 몫 금지**). 📁 balance.js `greatColdSnap` · state.js `frontWinterKey`. 🚦 **모달 골든 갱신 필수** · sim: 프론트 발령은 **겨울 고정일**(현 coldSnapForecast의 Math.random 회피). ⚠️ 기존 랜덤 한파와 이중 카운트 방지(상한 밖 별도).
  - **✅ 구현 확정 (2026-07-08, 디렉터 확정 2건: 규율 모달=하드/하드코어 전용 · 1개 선택-기간 지속)**: 예보 겨울 5일차(매일 카운트다운) → 발동 8일차, 3일(8~10일차). 노말 sev2 = 계절 의식(예보·연출·memoir만, 규율 자동 '그냥 버틴다') / 하드·하드코어 sev3 + 아침 보고 뒤 규율 모달 4택 — 배급 반(허기 감소 ×0.65 · 쾌적 -3) / 쪽잠 불침번(난방 연료 격일 · 취침 회복 -15) / 비상식량 개봉(통조림 +4 · 쾌적 -4) / 그냥 버틴다. 창을 그냥 닫으면 '그냥 버틴다' 확정(재오픈 루프 방지). 진행 중 랜덤 한파는 흡수(severity/until max), **`coldSnapForecast`는 불가침** — 지우면 겨울 총 압박일수가 줄어 하드코어 치명성 붕괴(사망 25%→5%, 20시드 실측). **sim 제외(`!_simRunning` 가드, regionVisits 선례)** — 프론트 활성 3일이 랜덤 한파 예보 롤을 멎게 해 sim 실현 한파 2.2→1.6회/겨울로 왜곡, 경제 핀(하드코어 사망 존재)이 깨진다. sim은 측정기(베이스라인 불가침), 실게임은 프론트가 얹혀 더 험함. **재도입 금지**: 프론트를 sim에 넣으려면 예보 롤 캐던스 보존을 먼저 풀 것. memoir: `acc.front`/`acc.frontDiscipline` → closing 앞 한 줄(규율 변주 3종). 검증: 코어 50종(프론트 5) + modal-golden `front_choice` + 실렌더 접지(예보→발동→모달→클릭→통조림 +4).
- **④ 부상 서사화** — ♻️ `winterSnap.acc` + `buildWinterMemoir` 페이지 스키마 + `pendingWinterMemoir` 큐 + 프로젝트 memoir(`memoirKey`) 통째. 🆕 winterSnap.acc 부상 통계 + memoir 부상/흉터 라인(`state.scars` 공유). 📁 i18n winter.memoir 부상 변주(im-not-strange-ai·keep-all). 🚦 무영향(종이 텍스트) · save 폴백 이미 `acc||{}`. ⚠️ 낮음 — '동작'(악화 심화)은 하드코어 전용, 기록 표시는 전 모드.

### 9.5 연구실 진실 + 엔딩 3분기

- ♻️ **재사용**: 1.4 research 메모 파이프라인 전체(`MEMOS_RESEARCH` 필터 · `tryDropMemoOnExpedition` research 우선 2.5배 게이트 · `collectMemo` · `pendingMemoPopup`→`showMemoPage` · 기록 탭 스포일러 게이트) · 엔딩 골격(`runEndingSequence` DOM+라인 페이저+Ending OST/SFX) · 인카운터 프레임(events.js special+textFn+choices) · doctor 스파인(`tryDoctorRadio`·`doctorFragmentsComplete`·`survivorLights`) · 9겨울 마일스톤.
- 🆕 **신규**: (1) 연구실 진실 메모군 — GD §4 '이관/선별·규합·초대' 4단(관찰기록→'합격'→규합계획→초대)은 아직 텍스트에 없음. lore.js 신규 region 메모 + (순서 보장 시) research 드랍을 **무작위 픽 → 미수집 최소 index 순차**로 변경. (2) 엔딩 3분기 — 9겨울(`state.winters===9`) 트리거 + **체류지 가중 계산 함수(현재 없음)** → 탈출/신세계/안식 choices 인카운터 + 엔딩별 시퀀스·톤. '탈출'은 조기(9겨울 전, doctor 스파인) 가능 — Day10000 게이트와 별개 트리거. **Day10000 = '먼 에필로그'로 격하**.
- 📁 **데이터**: lore.js 신세계/초대 메모 · events.js ending 3-choice + 조기 탈출 분기 · state.js `endingType`/체류지 누적 + save 마이그레이션 · balance.js 체류지 가중 · i18n ending.* 대량.
- 🚦 **게이트**: 엔딩 선택 인카운터 노출 게이트(pendingEvent 다중 가드) 회귀 · sim: 9겨울/체류지 가중 확정(랜덤 회피) · 「침묵」 조명 골든 갱신 · 기록 탭 100% 수집률 갱신.
- ⚠️ **리스크**: 조기 탈출이 9겨울 서사와 시점 충돌 · 3분기 가중은 '갑툭튀 선택지 금지'(STRATEGY §1.2) 안 어기게 **누적 신호 기반** · 구세이브 `endingSeen=true` 유저의 3분기 소급 처리 · rsc 톤(조용한 발견·두 줄) 유지.
  - **✅ 구현 확정 (2026-07-08 — 코어 완료, lore 초안 디렉터 감수 대기)**: ① 이관의 진실 4단(lore.js nw1~4 · citycore · 관찰→선별→규합→초대) — rsc 두 줄 구조·양가성 유지, **순차 드랍**(무작위 픽 폐지 — rsc 12종도 동반 순차화, 폭로 순서=수집 순서). ② 엔딩 3분기 — `endingLeaning()`(랜덤 0: 탈출=박사 스파인 / 신세계=진실 조각 14+ / 안식=정든 집·고양이·쾌적. **도시 체류 가중은 §9.8 이후 편입**), passWinter(winters≥9)가 예약→그날 밤 `ending_choice` 발화(문안만 성향 변주, 3택+보류 항상 개방·보류 시 매 봄 재방문), `runEndingSequence(type)` 3벌 시퀀스 — **세 갈래 모두 런 계속**(엔딩=서사 마침표, 방치형 정체성). ③ 조기 탈출 — 정기 교신 +7일 확정 제안(`early_rescue`, 랜덤 0), 보류 시 소진→9겨울 재방문. ④ Day10000 기존 ending = 먼 에필로그로 존속(무인자 runEndingSequence). state endingType/endingChoicePending/earlyRescueDay + 마이그레이션 + MIG 재핀(1719799765). 검증: 코어 62/62(엔딩 5종: 트리거/기록/성향/조기/순차) + i18n 1102 무결(신규 43키 ko+en). 잔여: lore·엔딩 카피 디렉터 감수 · 기록 탭 수집률 표기(nw 4종 포함 여부) 확인.
  - **✅ 검수 패스 (2026-07-08 — 디렉터 오더 "검수해")**: 결함 2건 검거·수정 — ① **9겨울 밤 경합 역전**: 같은 밤 `ending_choice`가 `doctor_radio`(첫 무전·복선)를 밀어내던 우선순위를 교정. 무전(라디오 보유 시)→이튿날 구조 순으로 복선이 초대에 앞선다. 라디오 미보유 시 무전만 보류(구조는 막지 않음). ② **기록 탭 citycore 섹션 부재**: nw 수집분이 총계에만 잡히고 목록엔 없는 유령 카운트 → research 문법으로 「도심 중심지」 섹션 신설(발견 후 노출, `record.regionCitycore`). 카피 조정 3건 — line4의 `{cat}` 태그(` · 고양이와 함께`)를 스탯 구획 뒤로 이동(newworld는 스탯 구획이 없어 제거, 고양이 문장은 line2에 존치), en 2곳 자연화(escape.line1, nw2 "set down item by item"). 검증: 코어 **63/63**(경합 케이스 신설) + 접지 20/20(인카운터 DOM·시퀀스 3벌 실렌더·기록 탭·en 전환) + i18n **1103** 무결 + modal-golden 7/7. 비고: 구 Day10000 시퀀스는 "당신" 2인칭 존대 잔존(end3는 수첩 1인칭 반말) — 재윤문 여부는 디렉터 판단 대기.

### 9.6 히든 루트 「침묵」

- ♻️ **재사용**: **대형 프로젝트 엔진**(data/projects.js 테이블 + core/projects.js 술어 + `investProject` + `applyProjectEffect` switch — '테이블 추가 + case 1개') · subwayHub/subwayOpen 상태·UI · **레이캐스트 히트 패턴 통째**(`bunkerStairsObj` 전역 + `setBunkerStairs` + `pickStairs`의 조상 visible 체크 = '시각 표시 없는 순수 히트 영역'의 완성 선례) · 종이 문서 presenter(`showMemoPage`/`showTruthPage`) → 박사의 문서 · 지하철 셸터 지오('올라가는 곳 없는 역'=출구 없는 기존 시각) + **붉은 비상등 PointLight 이미 존재** · `buildRailSegments`/벙커 계단 = **조건부 현장 지오 선례**(`projectSiteStage` 게이트).
- 🆕 **신규**: (1) 히든 터치 포인트 — `subwayHiddenObj` 전역+setter+`pickHidden`(pickStairs 복제) + pointerdown 디스패치 1줄, 게이트=subRail1~3 전부 `projectDone`, 더블탭/홀드. (2) 개척 대형 프로젝트 1개 — **단, when 스키마에 `projectDone` 게이트 부재 → `core/projects.js`의 `projectAvailable`에 술어 추가 필요 = '엔진 무수정' 계약을 깨는 유일 지점.** (3) 연구소 도달/유보 상태 (4) 박사의 문서(showTruthPage 복제) (5) 사일로 시퀀스(어둠·계기판·버튼·화이트아웃 모달) (6) 개척 후 계단/사다리 조건부 지오(`buildHiddenStair`, buildRailSegments식 게이트).
- 🔦 **조명**: subway buildEnv **조건부 dim 분기**(히든 상태일 때만 확 줄임, 남은 광원=붉은 비상등만). **무조건 어둡게 금지** — 일반 subway 거주(버섯재배/암시장 가독) 보존.
- 🚦 **게이트**: **골든 갱신 필수**(§5.1 명시 — 조명값 변경) · 히든 터치는 '시각 변화 0 = 골든 무충돌'이 요구사항 · 히든 개통 상태는 flags 주입 신규 골든 씬(z_bunker_backdoor 선례) · 사일로/문서 모달-골든 커버 · sim: 프로젝트/openSubwaySegment RNG 없음(**pickHidden에 Math.random 금지**).
- ⚠️ **리스크**: 사일로·연구소를 '탐험 씬/전투'로 만들면 캐논 위배 — **지도·모달·종이·버튼만**(신규 3D 셸터 씬 금지) · 히든 터치 히트 우선순위(placing→stairs→cat→avatar→item) 충돌 · UI 힌트 0이라 QA 재현 절차 별도.

### 9.7 공통 인프라 (전 블록 공통)

- ⚠️ **sim 결정론이 최우선 제약**: `resolveExpedition`/`processDay`의 RNG 스트림이 시드 시뮬 기준선(Day30 clear544/storm435 · Day60 밴드)의 load-bearing. 신규 `Math.random()`(조우 롤·소문·악화·엔딩 트리거)은 **반드시 `!_simRunning` 가드 또는 시뮬 기대값 모델 동시 확장**. 확정 게이트(day/winters 파생) 우선, 랜덤은 최후.
- 🗄️ **세이브 스키마**: 신규 state 필드(gun·scars·endingType·subwayHidden·hiddenReached·frontWinterKey…) 전부 save.js 마이그레이션 기본값 + **tests/core.test.cjs MIG_HASH 스냅샷 갱신**.
- 🖼️ **골든**: 지도 신규 마커(프리뷰 정합) · 지하철 히든 조명(golden:update) · 신규 모달(중상·자기규율·엔딩·사일로 → modal-golden). 순수 데이터/텍스트는 골든 무영향.
- 📐 **코어45**: REGIONS/DISTRICTS/INJURIES/BAL 해시 가드 갱신.

### 9.8 스코프 경고 — 4개 도시 (§6)

**현 아키텍처는 '단일 도시 내 구역(DISTRICTS) 이주' 전제** — `districtOf`·MAP·MAP_MARKERS·SHELTER_MAP 전부 flat, DISTRICTS 위 상위 계층 없음. '4개 도시 각 풀세트'는 콘텐츠 4배 + game.js 광범위 침습 + 세이브 대변경(`currentCity`)이라 **최대 스코프·최대 리스크**.

- 이주 파이프라인(`moveCostFor`/`moveToShelter`/cross-district 비용·시간·layouts 보존)은 ♻️ 재사용 — '도시 간'으로 코스트만 스케일업.
- **권장**: 풀세트 복제 대신 **도시별 파라미터화**(공유 엔진 + 도시별 데이터 세트 + `mapBiomeDataUrl` 도시별 seed) + 볼륨 축소(도시당 셸터 2~3·지역 3, §8 제안). '한 겨울 한 도시' 강제 이주가 **코지 축적(정든 집 stayDays·커스터마이즈)과 충돌** — 이주=리셋이면 코지 상실, 이월 규칙 신중히.

### 9.9 착수 순서 (의존성)

1. **지역·시간 게이트(§9.1)** — 도심 중심지가 총·중상·연구실·엔딩의 무대. **최우선.**
2. **깊이 3종(§9.4)** — 독립성 높음, 기존 시스템 확장이라 안전. 1.x/데모에서도 부분 착지 가능.
3. **적대 다이얼 + 총·중상(§9.2·§9.3)** — 도심 중심지 위에. sim 가드 동반.
4. **연구실 진실 메모(§9.5-1)** — 순수 데이터, 병행 가능.
5. **엔딩 3분기(§9.5-2) → 히든 루트(§9.6)** — 최후. 서사 총합 위에 얹힘.
6. **4도시(§9.8)** — 스코프 결정 후 별도 대형 트랙.

> **한 줄 결론**: 2.0의 대부분은 **기존 엔진 위 데이터+판정 훅**으로 착지한다(방호복→총, 한파→대한파, research 메모→연구실 진실, 벙커 계단 히트→히든 터치). 진짜 신규 엔진은 없고, 유일한 '엔진 수정'은 히든 루트의 `projectDone` 게이트 하나. 최대 위험은 코드가 아니라 **스코프(4도시)와 sim 결정론 가드**다.
