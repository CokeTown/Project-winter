[
  "944: if (cd.cleanMod) logs.clean.push({ name: t('comfort.log.cleanState', { n: Math.round(cd.cl",
  "952: if (cd.moodMod) logs.security.push({ name: t('comfort.log.mood'), v: `${cd.moodMod > 0 ? '",
  "1716: return Object.entries(cost).map(([id, n]) => `${resIcon(id)}${n > 1 ? '×' + n : ''}`).join",
  "3500: if (p.comfort) lines.push(`<span style=\"color:var(--good)\">${t('prep.comfort', { pct: Math",
  "3515: if (r.furnChance) lootBits.push(t('obs.lootFurn', { pct: Math.round(r.furnChance * 100) })",
  "8720: spoil = Math.floor(spoil) + (frac > 0 && Math.random() < frac ? 1 : 0); // frac=0이면 Math.r",
  "8721: if (spoil > 0) { resConsume('food', spoil); notes.push(t(seasonOf().id === 'summer' ? 'day",
  "11075: case 'gauges': state.hunger = state.thirst = state.energy = 100; if (state.injury) state.i",
  "11135: if (state.hunger < 100 || state.thirst < 100 || state.energy < 100) { state.hunger = state",
  "11643: starving: state.hunger <= 0 || state.thirst <= 0,"
]