// palette.js — 「Nine Winters」 픽셀 아트 마스터 팔레트 (초안 v1, Fable 작성 → 디렉터 튜닝)
// 목적: 렌더 포스트 셰이더의 균일 8단계 RGB 양자화를 '큐레이션 팔레트 스냅'으로 교체하기 위한 색표.
//   장르 정의(추위 속 온기)에 맞춰 램프마다 휴시프트를 넣음 — 밝은 끝=따뜻하게, 어두운 끝=차갑게.
//   = 픽셀 아티스트가 손으로 하는 '명암 램프' 규율. 디렉터는 hex만 고치면 됨(값 편집 = 즉 반영).
//
// 튜닝 가이드:
//   · 각 램프는 밝음→어둠 순. 그림자 끝을 원색보다 살짝 차갑게(파랑/보라) 두면 '손맛'이 난다.
//   · 색 수를 늘리면 그라데이션이 매끄럽고, 줄이면 더 '픽셀아트'다워짐(현재 ~44색).
//   · 강조색(ACCENT)은 절제 — 녹/불빛/희귀만. 남발하면 통일감이 깨진다.

export const PALETTE = {
  // ── 온기: 불씨·난로·램프 (실내 '마지막 불씨') ──
  ember: ['fff4dc', 'ffdfa0', 'f7b866', 'e08a44', 'b25f30', '814226', '4e2f2e', '301f28'],
  // ── 목재: 바닥·가구 ──
  wood: ['dca868', 'b8824a', '8a5c34', '5c3d26', '362a2a'],
  // ── 벽·석고 (그림자는 모브로 시프트) ──
  plaster: ['f2ead2', 'dbccac', 'b39a82', '86706a', '574850', '352930'],
  // ── 차가운 실외·하늘·눈 (핵겨울) ──
  cold: ['e8eef0', 'c2d0d4', '98abb2', '6e828a', '4c5c66', '33424e', '202c36', '141a20'],
  // ── 식생·이끼 (차갑게 가라앉힌 녹) ──
  green: ['a2ad6a', '77824a', '4c5a34', '2e381f'],
  // ── 절제된 강조: 녹·경고·불빛·희귀 ──
  accent: ['b04434', 'e2593a', '3f7a72', 'c9a24e'],
};

// 셰이더에 넘길 평탄 배열 (RGB 0~255). 순서는 무관(스냅은 최근접).
export const PALETTE_FLAT = Object.values(PALETTE).flat().map(h => [
  parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16),
]);
