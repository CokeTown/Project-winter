/* ============================================================
   core/shelter.js — 셸터/개조 술어 (game.js 모놀리스 분해 Phase 1)
   ------------------------------------------------------------
   현재 거처의 개조(mod) 보유 여부 등 상태 술어. 쾌적·한파·일일 처리·렌더가 전역 참조.
   (개조 소품 3D 빌더/설치 로직은 렌더라 game.js 잔류 — 여기는 순수 판정만.)
   ============================================================ */
import { state } from './state.js';

// 현재 셸터가 특정 개조를 갖췄는가 (state.mods[현재셸터]에 modId 포함).
export function hasMod(id) { return (state.mods?.[state.current] || []).includes(id); }
